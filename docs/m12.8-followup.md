# m12.8 follow-up — retired 2026-05-02

This tracking doc was retired in PR #69 after every item closed
end-to-end. The file is kept as a stub so existing references
from lane-experience writeups (`lane-experience-m12.8.x.md`,
`lane-experience-m12.8.z.md`, `lane-experience-m12.8-cleanup.md`)
and from `stage2-design.md` / `protocols.md` continue to land
somewhere meaningful instead of breaking. The lane writeups are
historical narratives and were left untouched.

## What was here

- **8 bugs + Gap 1** surfaced by the `/tmp/kaikai-portfolio-demo`
  external demo, with bug-by-bug root-cause narratives.
  Closed in m12.8.x phase 2 (`4b137d4`, `ae0df22`, `81e0306`)
  and Bug 8 (`unitless` leak) in `f0acda2`.
- **Bug 6 effects-prelude gap** closed in the phase 4 retake
  (`a251689`, `49b4bd7`). Stdout / Stderr atomic split shipped
  in phase 4b (`64b85fd`, `8b134bb`, `54d5364`, `ee49db2`).
- **Phase 3 demo final close**: `examples/portfolio/` and
  `examples/usd_to_eur/` wired into `make test` via
  `test-demos-core`.
- **Post-Core REOPEN** (2026-04-27): four typer/resolver/codegen
  bugs (`10a1e7b`, `257b247`, `b528655`, `4c61184`); a `_ -> k`
  wildcard audit across 10 walker passes (`7bd5a68`); rep
  invariant walker (b) for `__proto_X` shadowing; and the
  `map_expr_kind` shared-visitor refactor (`ca05cf9` + 10
  walker migrations).
- **"Possible audit" section** (post-Core ergonomic tightening
  candidates: `string_concat` / `array_make` /
  `int_to_string` / etc.) **relocated to**
  `docs/prelude-audit.md` 2026-05-02.

## Reading the original

Pre-retirement file content:

```
git show HEAD~2:docs/m12.8-followup.md       # before this stub
git log --all -- docs/m12.8-followup.md       # full history
```

(Adjust `HEAD~2` to the right offset; PR #69 has the retirement
commit.)

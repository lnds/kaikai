import collections.set
import core.io

# Set literal builds a Set; `contains` reflects membership; empty
# literal has size 0.
fn main() : Unit / Stdout {
  let s = %[ 1, 2, 3 ]
  let es : Set[Int] = %[]
  println("#{set.contains(s, 2)} #{set.contains(s, 9)} #{set.size(s)} #{set.size(es)}")
}

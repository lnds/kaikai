# `Vec` declares one `Dim` slot, so a currency habitant cannot inhabit
# it — the annotation is rejected at the slot, not accepted as a free
# brand.
import money

fn main() : Unit / Console = {
  let a : Vec[Real]<USD> = [1.0]
  print("no")
}

# Privacy leak in `pub fn` effect row: the function declares a
# row that includes a non-`pub` effect `Hidden` defined in the
# same module. A consumer cannot handle this effect because the
# name is private. The validator must report the leak at the row
# annotation.

effect Hidden { peek() : Int }

pub fn do_it() : Int / Hidden = peek()

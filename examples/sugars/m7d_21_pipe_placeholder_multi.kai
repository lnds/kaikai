# m7d §21 — multiple `_` in the same `|>` call are rejected so the
# lhs is never silently evaluated more than once.

fn add3(a: Int, b: Int, c: Int) : Int = a + b + c

fn main() : Int {
  let x = 5 |> add3(_, _, 1)
  x
}

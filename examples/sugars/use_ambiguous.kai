# m7e §25 negative: two effects in scope expose the same op name
# (`print`). The desugar pass refuses to pick one and forces
# explicit qualification.

effect MyEff {
  print(s: String) : Unit
}

use Stdout
use MyEff

fn main() : Unit / Stdout + MyEff {
  print("hi")
}

# m7d §23 — `++` is not generic. Both operands must be `String`,
# or both must be `[T]` (with the same `T`). Mixing a String with
# a list, or two unrelated list element types, is a type error.

fn main() : String {
  "x" ++ [1, 2, 3]
}

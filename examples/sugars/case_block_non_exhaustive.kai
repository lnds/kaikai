# Issue #415 — non-exhaustive case-block. The desugared `match`
# inherits the existing exhaustiveness check; missing variants must
# be reported. Here `B` is the uncovered variant.

type Color = R | G | B

fn name(c: Color) : String {
  case R -> "red"
  case G -> "green"
}

fn main() : Int = 0

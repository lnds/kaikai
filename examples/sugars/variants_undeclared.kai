# m7e §11 negative: variants[T]() rejects undeclared types. The
# typer cannot find any variants returning the requested tycon, so
# it reports "not a sum type" with a help that points at the
# canonical declaration form.

fn main() : Int {
  let xs = variants[NoSuchType]()
  0
}

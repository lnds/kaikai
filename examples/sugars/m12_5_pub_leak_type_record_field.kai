# Privacy leak in `pub type` record field: the public record
# `Account` has a field whose type `Token` is a non-`pub` type
# in the same module. A consumer cannot construct the record
# without naming `Token`. The validator must report the leak at
# the field's type annotation.

type Token = Tok(Int)

pub type Account = { id: Int, key: Token }

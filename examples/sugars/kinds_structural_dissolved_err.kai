# `Structural` grouped two kinds that never shared an engine, so it is
# gone: `Region` is classified by `Nominal`, `Shape` by `ConstructorApp`.
# The dissolved name no longer resolves.
kind Bad : Structural with bad

fn main() {
  print("no")
}

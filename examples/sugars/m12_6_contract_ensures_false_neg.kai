# m12.6.x #6 — same check applies to `ensures`. A folded-false
# postcondition is a contract that can never hold; the compiler
# rejects the program rather than emit a guaranteed-panic.

fn buggy(x: Int) : Int
  ensures 1 == 2
  = x

fn main() : Unit / Stdout = Stdout.print("never reached")

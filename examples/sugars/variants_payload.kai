# m7e §11 negative: variants[T]() rejects T whose variants carry
# payload. The diagnostic must list the offending variant names so
# the user knows which ones to factor out.

type Mixed = Foo | Bar(Int) | Baz | Qux(String)

fn main() : Int {
  let xs = variants[Mixed]()
  0
}

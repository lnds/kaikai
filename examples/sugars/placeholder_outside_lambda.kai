# Issue #385 — `.` outside a lambda-expected (call-argument)
# position must be rejected per `docs/kaikai-minimal.md`
# §"Placeholder rules": "`.` outside of lambda-expected context
# is a compile error."

fn main() : Unit / Console {
  let x = . + 1
  print(int_to_string(x))
}

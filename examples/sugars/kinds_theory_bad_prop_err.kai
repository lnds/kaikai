# A theory assembles properties from a closed menu; anything outside it
# is rejected at the declaration.
theory Bad = { assoc, banana }

kind Fruit : Bad with fruit

pub fn main() : Unit / Console = { print("unreachable") }

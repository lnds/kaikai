# A trailing parameter group with no `:` is rejected — kaikai requires
# annotations in public signatures, so an unannotated group is an error,
# not inference.
fn foo(a, b) : Int = a + b

fn main() : Int = foo(1, 2)

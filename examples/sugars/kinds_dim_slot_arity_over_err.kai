# `Matrix` declares two `Dim` slots, so a three-habitant annotation is
# rejected at the annotation on arity.
import math.linalg

fn main() : Unit / Console = {
  let m : Matrix[Real]<2, 3, 4> = linalg.of([[1.0, 2.0], [3.0, 4.0]])
  print("no")
}

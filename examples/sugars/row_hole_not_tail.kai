# m7f §6 negative: `?e` (or `?`) must be the last item in a row,
# same constraint as user-named row variables (`e + Stdout` rejects
# for the same reason).

fn bad() : Unit / ?e + Stdout = Stdout.print("nope")

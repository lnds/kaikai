# `Functorial` names a protocol-law set, not a unification theory, so it
# cannot classify a kind. The theory catalog holds only real engines.
kind Bad : Functorial with bad

fn main() {
  print("no")
}

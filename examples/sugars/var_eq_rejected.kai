# Negative — `var name = init` is rejected: `:=` is the single mark of
# mutability, so a cell declaration uses `:=`, never `=`. A bare `=`
# binds an immutable `let`.
fn main() : Int {
  var counter = 0
  counter := counter + 1
  counter
}

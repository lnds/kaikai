# A symbol where a `with Int` kind expects a value habitant is a
# spanned error naming the expected domain.
kind Slot : AbelianGroup with Int

fn bad(x: Real<Slot.USD>) : Int = 0

fn main() : Unit / Console = print("no")

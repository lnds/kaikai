# A value habitant needs a kind with its domain in scope to resolve
# into. `Int` values always resolve (the base `Dim` kind ships them);
# a `String` value habitant still needs a `with String` declaration.
fn bad(x: Real<"tag">) : Int = 0

fn main() : Unit / Console = print("no")

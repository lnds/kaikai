# Negative — `@counter` is rejected for a `var` cell: a cell reads
# NAKED. `@` is the deref of a first-class `Ref[T]` (OCaml `!r`), which
# a block-local cell is not.
fn main() : Int {
  var counter := 0
  counter := @counter + 1
  counter
}

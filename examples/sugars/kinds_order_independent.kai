# issue #1108: a habitant declared before its `kind` decl still resolves
# — the introducer pre-scan is order-independent.
metric m

kind Metric : AbelianGroup with metric

pub fn main() : Unit / Console = {
  let x = 3.0<m>
  print("order ok")
}

# m7d §10 — punning still requires the binding to exist in scope.
# `{ x }` expands to `{ x: x }`, so the resolver must find `x`.
# `nope` is never bound, so the desugared `{ x: nope }` form fails
# the same name-resolution check.

type Pt = { x: Int }

fn main() : Pt {
  Pt { nope }
}

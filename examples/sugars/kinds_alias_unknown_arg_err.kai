# An alias's unit-kind tparam argument must name a declared habitant:
# an unknown symbol diagnoses at the use site after expansion.
kind Cur : Module with cur

cur USD

type Amount[c: Cur] = Real<c>

fn main() {
  let a: Amount[Zorro] = 10.0<USD>
  print("no")
}

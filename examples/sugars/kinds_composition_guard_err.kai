# issue #1189: a `Composition` habitant stands alone — no product, no
# power. `be^2` is rejected at the annotation that spells it, the same
# formation guard `Module` currencies get.
fn bad_ann(x: U32<be^2>) : Int = 0

pub fn main() : Unit / Console = { print("unreachable") }

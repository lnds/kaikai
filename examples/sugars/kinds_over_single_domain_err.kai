# `over` names a second type domain, so a single-domain theory takes
# none: AbelianGroup's operation stays within units (`m/s^2` is one
# domain, however rich), and a scalar ring would have nothing to act on.
kind Bad : AbelianGroup over Int with bad

fn main() {
  print("no")
}

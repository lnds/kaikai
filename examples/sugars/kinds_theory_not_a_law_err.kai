# The reverse of the Functorial guard: a protocol header names a law
# set, so a unification theory is rejected there. Theories and laws are
# disjoint namespaces.
protocol Sequence[s: Shape] : AbelianGroup {
  fn map[a, b](xs: s[a], f: (a) -> b) : s[b]
}

fn main() {
  print("no")
}

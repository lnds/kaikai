# Shape composition `s[t[..]]` (two stacked shape variables) is exactly
# functor-compose — the case that reintroduces higher-order unification.
# It is rejected at formation; a user wanting it wraps the nesting in a
# concrete arity-1 `type`.
fn bad[s: Shape, t: Shape](xs: s[t[Int]]) : Int = 0

pub fn main() : Unit / Console = print("unreachable")

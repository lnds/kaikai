# `Matrix` declares two `Dim` slots, so a one-habitant annotation is
# rejected at the annotation on arity — a written `<>` names every slot.
import math.linalg

fn main() : Unit / Console = {
  let m : Matrix[Real]<2> = linalg.of([[1.0, 2.0], [3.0, 4.0]])
  print("no")
}

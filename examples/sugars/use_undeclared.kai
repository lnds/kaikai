# m7e §25 negative: `use NoSuchEffect` references an effect that
# is not declared anywhere in scope. The desugar pass emits a clear
# error before any rewrite happens.

use NoSuchEffect

fn main() : Int = 0

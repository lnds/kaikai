# Module theory formation guard, annotation side: a written `USD^2`
# is rejected at the annotation that spells it — habitants of a
# Module kind admit no products or powers.
kind Cur : Module with cur

cur USD

fn bad_ann(x: Real<USD^2>) : Real = 0.0

fn main() {
  print("no")
}

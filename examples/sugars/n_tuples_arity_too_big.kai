# Issue #154 — n-tuple sugar caps at arity 4. A 5-tuple literal must
# be rejected at parse time with a clear diagnostic pointing at the cap.

fn main() : Unit / Stdout {
  let bad = (1, 2, 3, 4, 5)
  Stdout.print(int_to_string(bad.fst))
}

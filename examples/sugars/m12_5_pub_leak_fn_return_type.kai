# Privacy leak in `pub fn` return type: the function exposes a
# private record type `Hidden` that the consumer of the module
# cannot name. The validator must report the leak at the return
# annotation.

type Hidden = H(Int)

pub fn wrap(n: Int) : Hidden = H(n)

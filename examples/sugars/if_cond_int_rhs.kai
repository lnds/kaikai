# No-regression: an `Int` RHS was never ambiguous (no UpperIdent before
# the `{`); it must keep compiling exactly as before the fix.

fn main() : Int / Console {
  let x = 0
  if x == 0 {
    print("zero")
  }
  0
}

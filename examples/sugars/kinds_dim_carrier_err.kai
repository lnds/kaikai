# The carrier guard: a fixed-width Vec index is an Int value habitant
# (`Dim`), never a symbol of another kind.
kind Tag : Module with tag
tag red

fn main() : Unit / Console = {
  let v : Vec[Real]<red> = [1.0]
  print("no")
}

# m12.5: Int<UserId> branding works the same as Real<USD>.

unit UserId

pub fn main() : Unit  / Console = {
  let alice : Int<UserId> = 42<UserId>
  let bob   : Int<UserId> = 100<UserId>
  print("int unit ok")
}

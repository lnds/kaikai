# m12.5: alias declaration. Newton = kg * m / sec^2 — the typer
# canonicalises the alias body when resolving Real<Newton>.

unit kg
unit m
unit sec
unit Newton = kg * m / sec^2

pub fn main() : Unit = {
  let f : Real<Newton> = 9.81<kg * m / sec^2>
  print("alias ok")
}

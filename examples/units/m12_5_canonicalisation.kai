# m12.5: canonicalisation. m * sec ≡ sec * m, so they must unify.

unit m
unit sec

pub fn main() : Unit  / Console = {
  let a : Real<m * sec> = 5.0<m * sec>
  let b : Real<sec * m> = a
  print("canon ok")
}

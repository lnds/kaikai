# m12.5: generic unit-polymorphic function.

unit m
unit USD

pub fn double[u: Unit](x: Real<u>) : Real<u> = x + x

pub fn main() : Unit  / Console = {
  let twice_m   : Real<m>   = double(5.0<m>)
  let twice_usd : Real<USD> = double(2.5<USD>)
  print("generic unit ok")
}

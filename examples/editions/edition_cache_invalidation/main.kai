# Issue #603 — cache partition smoke. Compiling under different
# edition values must place prelude `.kab` blobs in distinct
# subdirectories so cross-edition contamination is impossible. The
# accompanying `check.sh` exercises the partition.

fn main() : Unit / Console = print("ok")

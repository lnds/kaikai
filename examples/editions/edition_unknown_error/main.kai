# Issue #603 — `edition = "atlantis"` is not a known edition. The
# compiler rejects this before any compilation work runs, naming the
# valid editions so the user can correct the manifest.

fn main() : Unit / Console = print("never reached")

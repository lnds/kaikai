# Issue #1585 — a leading `-` is NOT a continuation: it can start an
# expression, so `r` and `- 2` stay two statements and the block's
# value is the final `-2`. Pins the pre-existing semantics.

fn t() : Int = {
  let r = 7
  r
  - 2
}

fn main() : Unit / Stdout = Stdout.print("#{t()}")

# Issue #1585 — inside parens a leading `-` cannot continue the
# expression; the diagnostic points at the newline, not the operator.

fn t(a: Int, b: Int) : Int = {
  (a
   - b)
}

fn main() : Unit / Stdout = Stdout.print("#{t(5, 2)}")

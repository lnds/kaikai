# Issue #1585 — the parenthesised multi-line predicate that used to
# fail even with the parens: `(x > 1` / newline / `and x < 10)`.

fn in_range(x: Int) : Bool = {
  (x > 1
   and x < 10)
}

fn main() : Unit / Stdout = {
  Stdout.print("#{in_range(5)} #{in_range(20)}")
}

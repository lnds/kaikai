# Issue #594 baseline regression guard for `||` (flat_map pipe).
# `core.list.flat_map` must be resolved through the head-owner cache
# without a hardcoded compiler entry.

fn main() : Unit / Console {
  let xs = [1, 2] || (n) => [n, n * 10]
  each(xs, (n) => print(int_to_string(n)))
}

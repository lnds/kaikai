# Issue #594 baseline regression guard for `|?` (filter pipe).
# `core.list.filter` must be resolved through the head-owner cache.

fn main() : Unit / Console {
  let xs = [1, 2, 3, 4] |? (n) => n > 2
  each(xs, (n) => print(int_to_string(n)))
}

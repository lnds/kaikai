effect Counter {
  get() : Int
}

fn ask() : Int / Counter = Counter.get()

fn main() : Unit = {
  let result = handle {
    ask()
  } with Counter {
    get(resume) -> resume(42)
    return(x)   -> x
  }
  exit(result)
}

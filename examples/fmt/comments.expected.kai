# Top-of-file documentation comment.
# Spans several lines and explains the module purpose.

# Helper that doubles its argument.
fn double(x: Int) : Int = x * 2

# Entry point for the demo.
fn main() {
  let n = 21
  # Print the doubled value.
  print(int_to_string(double(n)))
}

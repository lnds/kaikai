# The vertical pipe chain `kai info pipes` and the stdlib are written in.
fn pipeline(xs: [Int]) : [Int] = xs | (x => x * 2) | (x => x + 1)

fn selected(xs: [Int]) : [Int] = xs |? (x => x > 0) | (x => x * 10) |? (x => x < 1000)

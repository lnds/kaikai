# Import lists are already one-per-line; the fixture pins that they
# stay that way under the width model.
import loop.{while, until, repeat, forever}

import string_builder

fn spin(n: Int) : Int {
  var i := 0
  while { i < n } {
    i := i + 1
  }
  i
}

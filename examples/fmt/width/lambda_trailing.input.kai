# A trailing block lambda whose body overflows once it is emitted.
fn dump(rows: [String], label: String) : Unit / Stdout =
  each(rows) { row ->
    Stdout.print(
      string_concat_all([label, " row: ", row, " (", int_to_string(string_length(row)), ")"]))
  }

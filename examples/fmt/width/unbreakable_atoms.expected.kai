# CONTROL — lines no formatter can shorten. The metric must classify
# these as `hard` (indent + longest atom already exceeds the budget)
# and never charge them to the writer.
fn banner() : String = "one single string literal, over a hundred characters wide, that no line-breaking algorithm may split"

fn a_deliberately_long_identifier_that_all_on_its_very_own_consumes_more_than_the_whole_available_line_budget() : Int = 0

# A predicate the author broke across lines with leading operators.
fn allow(role: String, active: Bool, verified: Bool, age: Int) : Bool = if (((role == "admin") and active) and verified) and (age >= 18) {
  true
} else {
  false
}

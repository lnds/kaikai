# A call whose argument list the author wrapped over two lines.
fn join_names() : String = string_join(["alpha", "beta", "gamma", "delta", "epsilon", "zeta", "eta"], ", ")

fn report(name: String, count: Int) : String = string_concat_all(["module ", name, " exports ", int_to_string(count), " symbols"])

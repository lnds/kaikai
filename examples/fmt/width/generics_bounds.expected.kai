# Type parameters with bounds, plus a return row.
protocol Tagged {
  tag(self: Self) : String
}

fn describe_pair[a: Tagged + Eq, b: Tagged](left: a, right: b, sep: String) : String = string_join([left.tag(), sep, right.tag()], " ")

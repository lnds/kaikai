fn divide(a: Int, b: Int) : Int requires b != 0 = a / b

fn scaled(a: Int, b: Int) : Int requires b != 0 ensures result >= 0 = a / b * 0

fn both(a: Int) : Int requires a > 0 requires a < 100 = a + 1

fn plain_assert(a: Int) : Int {
  assert a > 0
  a + 1
}

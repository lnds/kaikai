fn main() : Unit / Console {
  let int_imag = 3i
  let real_imag = 2.5i
  let handwritten = complex.mk(1.5, 2.5)
  let zero_real = complex.mk(0.0, 4.0)
  print(real_to_string(int_imag.im))
  print(real_to_string(real_imag.im))
  print(real_to_string(handwritten.im))
  print(real_to_string(zero_real.im))
}

protocol Show {
  show(x: Self) : String
}

protocol Greet {
  hello(x: Self) : String
  formal(x: Self, prefix: String) : String
}

type Color = Red | Green | Blue

impl Show for Color {
  fn show(c: Color) : String = match c {
    Red   -> "red"
    Green -> "green"
    Blue  -> "blue"
  }
}

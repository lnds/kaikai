fn make_adder(n: Int) : (Int) -> Int = (x) => x + n

fn pair_op(f: (Int, Int) -> Int, a: Int, b: Int) : Int = f(a, b)

fn main() {
  let add5 = make_adder(5)
  let mul = (a, b) => a * b
  print(int_to_string(pair_op(mul, 3, 4)))
  print(int_to_string(add5(7)))
}

fn one(n: Int) : Int = n + 100

fn total(ys: [Int]) : Int = sum(ys)

pub fn nested(xs: [Int]) : [[Int]] = xs | (x => ([x] | (y => one(y))))

pub fn threaded(xs: [Int]) : [Int] = xs | (x => ([x, x] |> total))

fn main() : Int {
  println("#{nested([1, 2])}")
  println("#{threaded([3, 4])}")
  0
}

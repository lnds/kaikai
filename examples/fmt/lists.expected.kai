fn sum_first_n(n: Int) : Int =
  list_foldl([1..n], 0, (acc, x) => acc + x)

fn main() {
  let nums = [1, 2, 3, 4, 5]
  let doubled = nums |> map(x => x * 2)
  print(int_to_string(sum_first_n(10)))
}

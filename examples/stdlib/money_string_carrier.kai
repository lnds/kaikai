# The carrier takes no protocol bound: a String-carrier Money
# constructs and round-trips through of/amount. Its arithmetic is
# rejected — see money_string_carrier_add_err.
import money

pub fn main() : Unit / Console = {
  let s: Money[String]<USD> = money.of("ten-fifty")
  print(money.amount(s))
}

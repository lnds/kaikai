# issue #182 — smoke test for result_ok (Result -> Option of value).

fn pretty(o: Option[Int]) : String {
  match o {
    Some(v) -> "some=#{int_to_string(v)}"
    None    -> "none"
  }
}

fn main() {
  let good : Result[Int, String] = Ok(11)
  let bad  : Result[Int, String] = Err("nope")
  println(pretty(result.ok(good)))
  println(pretty(result.ok(bad)))
}

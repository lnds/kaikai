# m12.6.x #7 — negative fixture. The lexer rejects a regex sigil
# that hits a newline before the closing delimiter; multi-line
# patterns are out of scope for v1 (single-line only, mirroring the
# behaviour of single-line string literals).

type Bad = String where matches ~r/oops

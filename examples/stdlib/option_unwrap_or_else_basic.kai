# issue #182 — smoke test for opt_unwrap_or_else.

fn main() {
  let s : Option[Int] = Some(42)
  let n : Option[Int] = None
  println(int_to_string(opt_unwrap_or_else(s, () => 0)))    # inner wins
  println(int_to_string(opt_unwrap_or_else(n, () => 99)))   # f supplies
}

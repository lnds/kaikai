# issue #182 — smoke test for option.filter.

fn pretty(o: Option[Int]) : String {
  match o {
    Some(v) -> "some=#{int_to_string(v)}"
    None    -> "none"
  }
}

fn main() {
  let s : Option[Int] = Some(4)
  let n : Option[Int] = None
  println(pretty(option.filter(s, (x) => x > 2)))   # passes
  println(pretty(option.filter(s, (x) => x > 9)))   # fails
  println(pretty(option.filter(n, (x) => true)))    # None stays None
}

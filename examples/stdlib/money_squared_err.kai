# Money times money is inexpressible: Currency is a Module kind, so
# the product unit `USD^2` cannot be formed.
import money
import decimal as dec
import decimal_proto

fn main() {
  let a: Money[USD] = 10.50<USD>
  let b: Money[USD] = 2.00<USD>
  let c = a * b
  print("no")
}

# Negative: Semilattice habitants admit no product — `read * write`
# is a formation error, the join is spelled `read + write`.

fn main() : Unit / Stdout {
  let h : FileHandle<read * write> = file_handle(0)
  Stdout.print("no")
}

# Argument types don't matter: any positional call on a record type
# is rejected uniformly, swapped args included.

type User = { name: String, age: Int }

fn main() : Unit / Console = {
  let c = User(36, "ada")
  print(c.name)
}

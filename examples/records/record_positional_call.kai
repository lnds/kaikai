# A record type name is not callable: positional construction must
# use the record-literal form, so `User(...)` is a typer error.

type User = { name: String, age: Int }

fn nom(u: User) : String = u.name

fn main() : Unit / Console = {
  let b = User("ada", 36)
  print(nom(b))
}

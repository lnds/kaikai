# issue #1137 negative — a range pattern bound must be an Int/Char
# literal. A variable upper bound keeps exhaustiveness undecidable, so
# the parser rejects it.

fn f(n: Int, hi: Int) : String = match n {
  1..hi -> "in"
  _     -> "out"
}

fn main() : Unit / Stdout = Stdout.print(f(5, 9))

# `Int.MIN / -1` has no representable quotient: both backends trap
# "divide overflow" instead of running the hardware `sdiv` UB.
fn main() : Int / Stdout = {
  Stdout.print("before")
  let min = -9223372036854775807 - 1
  let d = -1
  Stdout.print(int_to_string(min / d))
  0
}

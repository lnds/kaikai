# A plain `Int` literal past i64 range is a hard compile-time error, not
# a silently wrapped value. The diagnostic points at the BigInt escape
# hatch (`n` suffix or `bigint.from_string`).

fn main() : Int / Stdout = {
  let x = 9223372036854775808
  Stdout.print(show(x))
  0
}

# A hex literal wider than 64 bits is a hard compile-time error, not a
# silently truncated value. The full 64-bit pattern is legal (see
# hex_literal.kai); 17+ significant hex digits overflow the width.

fn main() : Int / Stdout = {
  let x = 0x1FFFFFFFFFFFFFFFF
  Stdout.print(show(x))
  0
}

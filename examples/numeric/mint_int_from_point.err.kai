# A point literal cannot be coerced to an integer type — no silent
# truncation. `0.2` annotated `: Int` is a compile-time error.

fn main() : Int / Stdout = {
  let n : Int = 0.2
  Stdout.print(show(n))
  0
}

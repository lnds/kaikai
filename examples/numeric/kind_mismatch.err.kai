# Fixed-width integers are kind-distinct from Int (numeric lane A): an
# Int32 value does NOT unify with an Int annotation, so this is a static
# type error, not a silent coercion. Conversion is explicit via
# int32_to_int / int_to_int32.

fn main() : Int = {
  let x : Int = 42i32
  x
}

# A `d` literal whose magnitude exceeds the Int128 Decimal carrier is a
# fold-time error pointing at DecimalBig.

import decimal as d

fn main() : Int / Stdout = {
  let x : d.Decimal = 1701411834604692317316873037158841058270d
  Stdout.print(d.to_string(x))
  0
}

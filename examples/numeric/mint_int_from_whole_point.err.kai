# The point form declares fractional intent, so `3.0 : Int` is an error
# even though the value is whole — annotate `: Real` or drop the point.

fn main() : Int / Stdout = {
  let n : Int = 3.0
  Stdout.print(show(n))
  0
}

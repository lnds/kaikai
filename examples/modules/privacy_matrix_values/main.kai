# Negative: bare call, higher-order reference, interpolation region,
# and axiom call against non-pub value symbols of `lib`.
import lib

fn call_it() : Int = hidden()
fn hof() : Int = { let g = hidden
  g() }
fn interp() : Unit / Stdout = Stdout.print("#{hidden()}")
fn ax() : Int = secret_ax(1)

fn main() : Unit / Stdout {
  Stdout.print("should-not-compile")
}

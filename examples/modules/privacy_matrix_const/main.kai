# Negative: a non-pub const from a module whose only other decl is a
# pub const. Attribution must come from the import structure, not
# from streaming past a neighbouring fn.
import lib

fn main() : Int = SECRET

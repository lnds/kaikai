# Issue #510 — positive fixture: a `pub type T = A | B` exports
# both the type name and every variant constructor, so user code
# importing this module sees `T`, `A`, and `B`.

pub type T = A | B

pub fn show_t(t: T) : String = match t {
  A -> "A"
  B -> "B"
}

import dominio

fn main() {
  let t : dominio.Nota = dominio.Nota { id: 7 }
  let r : dominio.Respuesta = dominio.OtherResp
  match r {
    OkResp(s)  -> print("id=#{t.id} s=#{s}")
    OtherResp  -> print("id=#{t.id} s=other")
  }
}

# Negative: lib.Storage is not pub, so referencing it in main's
# row annotation must be rejected.

import lib

fn do_it() : Int / Storage = peek()

fn main() : Unit / Stdout {
  Stdout.print("should-not-compile")
}

# Negative cross-module: `lib.Token` is not `pub`, so naming it
# in a fn signature from `main` must be rejected at the use site.

import lib

fn unwrap(t: Token) : Int = match t { Tok(n) -> n }

fn main() : Unit / Stdout {
  Stdout.print("should-not-compile")
}

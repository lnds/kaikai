# Cross-module type privacy: `Token` is module-private. A consumer
# that imports `lib` cannot name `Token` in a signature or body
# reference. The exporter only uses it internally.

type Token = Tok(Int)

pub fn make() : Int = 42

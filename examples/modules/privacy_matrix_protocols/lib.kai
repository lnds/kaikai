# A non-pub protocol must be invisible to importers: no foreign
# impls, no bare op calls, no tparam bounds naming it.
protocol Sneaky {
  peek(x: Self) : Int
}
pub type Box = { v: Int }
impl Sneaky for Box {
  fn peek(x: Box) : Int = x.v
}
pub fn make() : Box = Box { v: 9 }

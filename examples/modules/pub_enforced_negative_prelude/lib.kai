# Issue #510 — negative fixture loaded via --prelude. Same shape
# as pub_enforced_negative_import, but the call site does NOT
# import `lib` explicitly; the test harness passes it as a
# --prelude file. The validator must still reject `lib_private`.

pub fn lib_public() : Int = 42

fn lib_private() : Int = 99

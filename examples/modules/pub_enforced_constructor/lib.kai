# Issue #510 — non-`pub` sum type. T's variants A and B are
# unreachable from outside this module: the importing main.kai
# must fail with `is private to module 'lib'` on any of T, A, B.

type T = A | B

pub fn lib_public() : Int = 42

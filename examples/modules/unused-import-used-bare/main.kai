import p
import q

# Every import here is used: `p` names `k` qualified, `q` calls its
# `twice` bare after `p` has already loaded it. None is reported.
fn main() : Unit / Stdout = Stdout.print("#{p.f()} #{q.g()}")

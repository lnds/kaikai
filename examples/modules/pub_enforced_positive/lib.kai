# Issue #510 — positive fixture for `pub` enforcement.
# `lib_public` is exported; `lib_private` is intentionally not
# `pub`, so it must not be visible from outside this file.

pub fn lib_public() : Int = 42

fn lib_private() : Int = 99

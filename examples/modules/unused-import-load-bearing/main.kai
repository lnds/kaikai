import p
import q

# `q` calls `twice` and `plus_one` bare and imports neither: `k` enters the
# unit only through `p`, and `b` only through `a`, which `p` imports. Those
# two imports are load-bearing; `d` is the one `p` can drop.
fn main() : Unit / Stdout = Stdout.print("#{p.f()} #{q.g()}")

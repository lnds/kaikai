# Negative: every type-reference position against non-pub types of
# `lib` — record construction, let annotations, sum ctor in
# expression and pattern position, signatures, and a local type
# wrapping a foreign private.
import lib

fn rec_ctor() : Int = { let r = HiddenRec { hf: 1 }
  r.hf }
fn rec_annot() : Int = { let x : HiddenRec = panic("no")
  0 }
fn alias_annot() : Int = { let m : Meters = 3
  m }
fn ctor_expr() : Int = match Tok(3) { Tok(n) -> n }
fn sig(t: Token) : Int = 0
type Mine = { t: Token }

fn main() : Unit / Stdout {
  Stdout.print("should-not-compile")
}

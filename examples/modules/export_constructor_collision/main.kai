import dominio

fn main() {
  let t : dominio.Nota = dominio.Nota { id: 1 }
  let r : dominio.Respuesta = dominio.OkResp("done")
  match r {
    OkResp(s)  -> print("id=#{t.id} s=#{s}")
    OtherResp  -> print("id=#{t.id} s=other")
  }
}

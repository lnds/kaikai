# Cross-module const privacy: `SEED` is module-private, so it stays
# out of the export set even though `pub const LIMIT` next to it
# crosses. The exporter only reads `SEED` internally.

const SEED : Int = 7

pub const LIMIT : Int = 99

pub fn seeded(n: Int) : Int = n + SEED

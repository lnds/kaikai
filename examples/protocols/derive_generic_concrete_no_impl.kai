# A generic type's parameters are covered by the derived impl's bound,
# but a field of a concrete type still needs that type's own impl.

type Tag = TA | TB

#[derive(Eq, Ord)]
type Slot[k] = Empty | Full(k, Tag)

fn main() : Unit / Stdout = Stdout.print("unreachable")

# m12.8: protocol Hash example over String.

protocol Hash {
  hash(x: Self) : Int
}

impl Hash for String {
  fn hash(s: String) : Int = string_length(s)
}

fn main() : Int {
  print(int_to_string(hash("abc")))
  print(int_to_string(hash("hello, world")))
  0
}

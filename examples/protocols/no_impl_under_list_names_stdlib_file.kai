# A missing impl found inside a stdlib container impl is reported in the
# stdlib file that holds the call, not under the user's file name with
# the stdlib's line number.

type Box = Box(Int)

fn main() : Int {
  let xs : [Box] = [Box(1)]
  let _ = show(xs)
  0
}

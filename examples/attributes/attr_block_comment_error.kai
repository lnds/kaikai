# Issue #608: negative fixture — `/*` is reserved and produces TkError;
# kaikai follows Elixir on not supporting block comments.

/* this opening sequence is not allowed */
fn main() : Unit / Console = print("never reached")

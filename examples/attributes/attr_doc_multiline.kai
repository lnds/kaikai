#[doc("""
Multi-line attribute lexing: a doc attribute that spans several
physical lines and carries a multibyte char — an em-dash — must be
skipped whole by the lexer, not truncated at the first newline. Kept
in minimal syntax so the stage1 lexer exercises it too.
""")]
fn main() {
  print("ok")
}

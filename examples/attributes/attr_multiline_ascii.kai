#[doc("""
Plain-ASCII multi-line attribute: it spans physical lines and its body
carries a ] bracket, a [ too, and "quotes". The lexer must skip the
balanced group whole, not stop at a bracket or quote inside a string.
Kept in minimal syntax so the stage0 and stage1 lexers exercise it too.
""")]
fn main() {
  print("ok")
}

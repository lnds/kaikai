# Issue #608: negative fixture — bare `#derive(...)` (pre-#608 syntax)
# now produces a deprecation TkError pointing at the new `#[derive(...)]`
# form. The same shape applies to bare `#unstable`.

#derive(Show)
pub type Point = { x: Int }

fn main() : Unit / Console = print("never reached")

# Issue #608: negative fixture — `//` is reserved and produces TkError
# with guidance pointing at `#` as the comment form.

fn main() : Unit / Console = {
  // this is not a kaikai comment
  print("never reached")
}

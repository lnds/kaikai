# Issue #487 fixture — Template 5 (missing effect).
# `pure_fn` declares no row but performs `print`, which carries
# `Stdout`. The body-vs-declared check fires; the collected
# Diagnostic carries notes for the enclosing row, the missing
# effect, and three remediation help lines.

fn pure_fn() : Int {
  print("hello")
  0
}

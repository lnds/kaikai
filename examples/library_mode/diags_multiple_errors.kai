# Issue #487 fixture — multiple T1-T5 diagnostics collected from a
# single compile. Order: T3 (unbound name) first (Env pre-pass), then
# T4 (wrong arity) from the typer in source order. The fixture pins
# that order so an LSP client can rely on a stable list.

fn add(x: Int, y: Int) : Int = x + y

fn main() : Int / Console {
  print(int_to_string(add(1)))
  print(missing)
  0
}

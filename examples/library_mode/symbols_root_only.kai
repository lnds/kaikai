const LIMIT : Int = 10

pub fn add(x: Int, y: Int) : Int = x + y

type Point = { x: Int, y: Int }

effect Ping {
  ping() : Unit
}

fn main() {
  let p = Point { x: add(1, 2), y: LIMIT }
  p.x
}

test "add adds" {
  assert add(1, 2) == 3
}

# @probe symbols 1:1

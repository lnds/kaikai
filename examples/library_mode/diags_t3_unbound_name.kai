# Issue #487 fixture — Template 3 (cannot find name in this scope).
# `missing_name` is unbound; the collected Diagnostic has no
# did-you-mean help because no scope name is within Levenshtein
# distance 3.

fn main() : Int / Console {
  print(missing_name)
  0
}

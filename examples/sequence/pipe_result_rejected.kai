# issue #201 Constraint #1 — `Result` is also outside the pipe
# dispatch table. The diagnostic mirrors the Option one but points
# at `result_and_then` instead of `opt_and_then`.

fn main() : Int {
  let bad = Ok(5) || (x) => Ok(x * 2)
  0
}

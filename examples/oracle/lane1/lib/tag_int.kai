# Independence oracle (#962 Lane 3) — Lane 1 (root_fns) probe library.
#
# The sibling `pub fn tag` over `Int`. Same bare name as `tag_str.tag`
# and as the adversary's root `fn tag`, so the imported `lib` module's
# bare call has a genuine 2-candidate narrowing decision.

#[doc("Tag an Int — the other narrowing candidate for the bare `tag`.")]
pub fn tag(n: Int) : String = "INT"

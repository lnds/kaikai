# A record field whose missing `Hash` sits under an Option is reported
# at the declaration, like a direct field.

type NoHash = { n: Int }

#[derive(Eq, Hash)]
type Keyed = { k: Option[NoHash] }

impl Eq for NoHash {
  fn eq(a: NoHash, b: NoHash) : Bool = a.n == b.n
}

fn main() : Int = 0

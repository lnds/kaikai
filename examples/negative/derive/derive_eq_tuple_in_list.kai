# A tuple is a `Pair` record, which has no `Eq`; one sitting under a list
# is reported at the declaration with the path down to it.

#[derive(Eq, Show)]
type InList = A([(Int, String)]) | B

fn main() : Int = 0

# A field whose missing `Eq` sits under a list is reported at the
# declaration, naming the path down to the type that lacks it.

type NoEq = { n: Int }

#[derive(Eq)]
type Wrapped = Many([NoEq]) | One

fn main() : Int = 0

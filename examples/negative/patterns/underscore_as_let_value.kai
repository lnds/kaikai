# Negative fixture (patterns): `_` as a `let` initialiser unified with
# any expected type and evaluated to zero. The binder `_x` marks an
# unused binding; `_` alone is not a value.

fn main() : Unit / Stdout = {
  let x = _
  Stdout.print("#{x}")
}

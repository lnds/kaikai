# `bytes` yields `[Byte]` and `from_chars` consumes `[Char]`, so
# feeding one to the other is a type error rather than a silent
# double-encode of every non-ASCII byte. `from_bytes` is the
# inverse of `bytes`.

fn main() : Unit / Console {
  print(string.from_chars(string.bytes("café")))
}

# A local name for a closure parameter is as opaque as the parameter.

fn g(n: Int, f: (Int) -> Unit / Mutable) : Unit = {
  let k = f
  k(n)
}

fn main() : Unit / Stdout + Mutable = {
  let a = array_make(2, 0)
  g(0, (i) => { a[i] := 7 })
  Stdout.print("a[0]=#{a[0]}")
}

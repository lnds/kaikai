# Every non-comment, non-empty line below must appear in stderr
# (fixed-substring match, order-independent). Column 37 is inside the
# SECOND interpolation — the one that failed — so this fails if part
# offsets are not threaded across the string.
error: type mismatch in function call
caret_interp_second_part.kai:10:37
  println("a=#{double(1)} b=#{double(s)}")
                                    ^
in argument 1 of `double`

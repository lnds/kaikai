# Every non-comment, non-empty line below must appear in stderr
# (fixed-substring match, order-independent). Column 35 is inside the
# INNER interpolation, so this fails if the two rebases do not compose.
error: type mismatch in function call
caret_interp_nested.kai:10:35
  println("outer #{"inner #{double(s)}"}")
                                  ^
in argument 1 of `double`

# Every non-comment, non-empty line below must appear in stderr
# (fixed-substring match, order-independent). Locks the caret onto the
# call the user wrote inside the interpolation, not onto the fragment's
# own line 1. The `^` column pins the argument, not the whole string.
error: type mismatch in function call
caret_interp_call_arg.kai:14:20
  println("#{double(s)}")
                   ^
in argument 1 of `double`

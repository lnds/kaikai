# Every non-comment, non-empty line below must appear in stderr
# (fixed-substring match, order-independent). Line 13 is inside the
# triple-quoted body, two lines below the one that opens the literal,
# so this fails if newlines within the literal do not move the line.
error: type mismatch in function call
caret_interp_triple_quoted.kai:13:16
value: #{double(s)}
               ^
in argument 1 of `double`

# Every non-comment, non-empty line below must appear in stderr
# (fixed-substring match, order-independent). Locks the anchor at the
# user's declaration and the "would never run" wording; the older
# behaviour blamed a stdlib call site for a privacy violation.
error: function 'print' shadows the builtin 'print'
builtin_shadow_fn.kai:7
so this body would never run
help: rename it

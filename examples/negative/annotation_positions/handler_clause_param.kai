pub type Pair[t, u] = Pair(t, u)
effect Sink {
  put(p: Pair[String, Bool]) : Unit
}
fn main() : Int = handle {
  Sink.put(Pair("a", true))
  0
} with Sink {
  put(p: Pair[Int, Bool], resume) -> resume(())
  return(x) -> x
}

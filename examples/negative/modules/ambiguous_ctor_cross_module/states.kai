import msgs

pub type SessionState = Cold | Ready

# The ambiguous reference lives in a module body, where no expected
# type chooses between the two `Ready` constructors.
pub fn pick() : Int = {
  let s = Ready
  0
}

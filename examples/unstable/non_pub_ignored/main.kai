# `#unstable` must precede a `pub` declaration. Marking a non-pub
# (i.e. private) fn is meaningless — the API is not exposed in
# the first place — so the parser rejects the form.

#[unstable]fn helper() : Int = 42

fn main() : Unit / Stdout = Stdout.print(int_to_string(helper()))

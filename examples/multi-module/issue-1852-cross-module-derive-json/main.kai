import encoding.json.{json_encode}
import inner
import outer

fn main() : Unit / Console = {
  let o = outer.Outer { i: inner.Inner { n: 42 },
                        ls: [inner.Inner { n: 1 }, inner.Inner { n: 2 }] }
  let s = json_encode(to_json(o))
  print(s)
  print(outer.decode_outer(s))
}

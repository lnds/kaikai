# m11 v1.x Template 5 — single missing effect. `pure_fn` is
# declared with the empty row but its body calls `print`, which
# performs `Stdout`. The diagnostic should pinpoint the call site,
# name the enclosing function, surface the empty enclosing row,
# and offer both "declare the effect" and "handle locally" help.

fn pure_fn() : Int = {
  print("hi")
  0
}

fn main() : Int = pure_fn()

# m11 v1 Template 3 regression — the typo is too far from any
# in-scope name (Levenshtein distance > 3 from anything visible),
# so the help line must be suppressed rather than emit a generic
# placeholder.

fn main() : Int = {
  let n = zzqxwvutsrq()
  print(int_to_string(n))
  0
}

# m11 v1.x Template 4 — calling a unary function with zero
# arguments. The diagnostic should still name the callee, report
# the declared arity, and surface the signature.

fn negate(x: Int) : Int = 0 - x

fn main() : Int = {
  let n = negate()
  print(int_to_string(n))
  0
}

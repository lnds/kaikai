# Triple-quote string literal: emitter must escape the body into a
# single C string literal (issue #311). Pre-fix, codegen printed the
# raw `"""..."""` source span into the generated C, breaking the
# preprocessor.

fn main() : Unit / Console {
  let m = """
    First line
    Second line
    """
  print(m)
}

fn maybe_number(has_value: Bool, n: Int) : Option[Int] {
  if has_value { ?present } else { ?missing }
}

fn main() {
  match maybe_number(true, 42) {
    Some(n) -> print(int_to_string(n))
    None    -> print("nothing")
  }
}

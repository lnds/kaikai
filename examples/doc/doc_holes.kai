# Fixture for the --holes-json `doc` field (issue #681): the hole's
# surrounding declaration carries a #[doc(...)] whose text must appear
# verbatim in the hole's JSON object.

#[doc("Documented holder of the probe hole.")]
fn holder(x: Int) : Int = ?h

fn main() : Int = holder(1)

# KIR fixture: atoms, direct calls, ANF flattening, let-binding.
fn add(a: Int, b: Int) : Int = a + b

fn triple(n: Int) : Int = add(n, add(n, n))

fn main() : Unit / Console = {
  let r = triple(7)
  Stdout.print(int_to_string(r))
}

import sa.{Box, sa_mk}
import sb.{Crate, sb_mk}

fn main() : Unit / Stdout = {
  let a = sa_mk()
  let b = sb_mk()
  let x = match a { sa.Node(n) -> n
                    sa.Leaf -> 0 }
  let y = match b { sb.Node(s) -> s
                    sb.Leaf -> "-" }
  Stdout.print("x=#{x} y=#{y}")
}

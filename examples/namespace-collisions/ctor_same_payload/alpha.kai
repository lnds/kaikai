pub type AlphaError = RepositoryFailure(String)

pub fn alpha_make(m: String) : AlphaError = RepositoryFailure(m)

pub fn alpha_show(e: AlphaError) : String = match e {
  RepositoryFailure(m) -> "alpha:#{m}"
}

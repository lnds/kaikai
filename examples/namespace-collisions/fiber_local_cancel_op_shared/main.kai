import guard
import core

fn main() : Unit / Stdout = {
  let out = handle { "#{guard.ask()} #{core.worker()}" } with Guard {
    raise(resume) -> resume("user-raise")
    return(x) -> x
  }
  Stdout.print(out)
}

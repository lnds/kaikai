# Two same-named effects from different modules must push distinct
# runtime evidence labels. Today both push the bare "Ask".
kai_evidence_push_with_jmp(&_node, "a::Ask"
kai_evidence_push_with_jmp(&_node, "b::Ask"

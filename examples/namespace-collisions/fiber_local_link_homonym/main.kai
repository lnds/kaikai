import wire
import core

fn main() = {
  let out = handle { "#{wire.join("a", "b")} #{core.exit_of_child()}" } with wire.Link {
    link(a, b, resume) -> resume("user-link:#{a}#{b}")
    return(x) -> x
  }
  Stdout.print(out)
}

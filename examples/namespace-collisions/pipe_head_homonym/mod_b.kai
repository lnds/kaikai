# Second declarer of the same head name `Source[t]`.

pub type Source[t] = MkSrcB(t)

pub fn map[a, b, e](s: Source[a], f: (a) -> b / e) : Source[b] / e {
  match s {
    MkSrcB(v) -> MkSrcB(f(v))
  }
}

pub fn unwrap[a](s: Source[a]) : a {
  match s {
    MkSrcB(v) -> v
  }
}

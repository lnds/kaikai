import ia

# `ib` is on disk and exports a `Cfg`, but this file never imported it.
# An unimported qualifier stays unknown even when the name it reaches
# for exists elsewhere in the package.
fn nm(c: ib.Cfg) : String = c.name

fn main() : Unit / Stdout = Stdout.print("#{ia.mk().level}")

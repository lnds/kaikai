import la
import lb

# la.Header and lb.Header are distinct nominal types with distinct
# layouts; one does not flow where the other is expected.
fn main() : Unit / Stdout = Stdout.print("#{la.port(lb.mk())}")

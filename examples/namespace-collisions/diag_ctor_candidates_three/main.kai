import qa
import qb
import qc

# Three homes, not two: the candidate list has to name every module that
# exports the name, so a diagnostic that stops at the first pair it finds
# sends the reader to a qualifier that is still ambiguous.
fn main() : Unit / Stdout = {
  let t = Tok(1)
  Stdout.print("x")
}

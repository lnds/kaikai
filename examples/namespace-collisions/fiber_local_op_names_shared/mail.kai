pub effect Mail {
  send(m: String) : Unit
  receive() : String
  self() : String
}

pub fn post(m: String) : Unit / Mail = Mail.send(m)
pub fn fetch() : String / Mail = Mail.receive()
pub fn who() : String / Mail = Mail.self()

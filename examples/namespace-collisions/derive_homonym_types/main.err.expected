duplicate impl: `impl Show for Item`
Two modules each declaring their own type with its own #[derive(Show)]
must not collide. Until that is fixed the program does not compile;
main.out.expected holds the behaviour it must reach.

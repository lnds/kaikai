import sa
import sb

fn label(o: Option[String]) : String = match o {
  Some(sb.CODE) -> "sb"
  Some(sa.CODE) -> "sa"
  Some(_) -> "other"
  None -> "none"
}

fn main() : Unit / Stdout =
  Stdout.print("#{label(Some("alpha"))} #{label(Some("beta"))} #{label(Some("gamma"))} #{label(None)}")

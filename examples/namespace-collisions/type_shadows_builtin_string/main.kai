import ba

# `String` is a builtin. The qualified reference names the module's own
# declaration and must select it; today the annotation resolves to it
# while the use site collapses to the builtin.
fn width(s: ba.String) : Int = s.chars

fn main() : Unit / Stdout = Stdout.print("#{width(ba.mk())}")

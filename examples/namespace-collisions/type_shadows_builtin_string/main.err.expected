shadows the builtin type
A user type sharing a builtin's spelling is refused for functions
(`neg_shadow_prelude_fn`); for types it is accepted and then honoured
inconsistently between the annotation and the use site.

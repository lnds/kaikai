# The payloads differ (record vs list), so the argument alone could pick
# a home; the bare use is still an error — position, not inference,
# resolves a constructor.
import pa
import pb

fn main() : Unit / Stdout = {
  let t = Tok([1, 2])
  Stdout.print("x")
}

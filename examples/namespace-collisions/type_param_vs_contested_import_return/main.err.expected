a type parameter is not a contested import
On a `type` declaration the binder wins; on a `fn` signature the
contested-import check runs first and reports `bare name 'T' is
exported by multiple modules`, advising a qualification the user
cannot write for their own parameter.

expected: (db.Item) -> Int
found:    (da.Item) -> ?t1
Today the message leaks the home spelling the compiler mints for a
contested type (`Item__db`, `Item__da`); DIAG.expected holds the target
behaviour, the name as the user wrote it.

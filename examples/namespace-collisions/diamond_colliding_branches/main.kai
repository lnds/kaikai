import dbase
import dleft
import dright
import dtop

fn main() : Unit / Stdout = {
  let r = dright.Failure(true)
  let l = dleft.Failure(1)
  let b = dbase.Failure("x")
  Stdout.print("#{dtop.show(dleft.wrap("y"))} #{dtop.rshow(r)} #{dtop.lshow(l)} #{dtop.show(b)}")
}

import dbase
import dleft
import dright

pub fn show(e: dbase.Err) : String = match e { Failure(m) -> "base:#{m}" }
pub fn lshow(e: dleft.LErr) : String = match e { Failure(n) -> "left:#{n}" }
pub fn rshow(e: dright.RErr) : String = match e { Failure(b) -> "right:#{b}" }

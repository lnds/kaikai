duplicate impl: `impl Eq for Entry`
Two modules each deriving Eq for their own same-named type must not
collide. The types themselves already scope correctly; the derived
impl is keyed by (protocol, type-name) without the type module.

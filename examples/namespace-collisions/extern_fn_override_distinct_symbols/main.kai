import da
import db

# One kaikai spelling, two different C symbols. The overrides keep the
# bindings apart at the boundary, so neither shadows the other.
fn main() : Unit / Stdout + Ffi = Stdout.print("#{da.a(0.0)} #{db.b(0.0)}")

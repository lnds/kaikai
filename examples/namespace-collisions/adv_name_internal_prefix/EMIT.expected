# A user function named after a compiler internal is NOT a hijack: the
# kaiu_ prefix keeps user symbols in their own space, so this emits
# kaiu_ha__kai_internal_drop while the runtime keeps kai_internal_drop.
# The lock is that user names never reach the kai_ space.
kaiu_ha__kai_internal_drop

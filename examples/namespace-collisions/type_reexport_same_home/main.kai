import rbase
import rmid

# The type arrives through its origin and through a module that only
# passes it along. Its home is where it was declared, so both routes
# name one type and the bare form stays unambiguous.
fn take(c: Cfg) : String = rmid.passthru(c)

fn main() : Unit / Stdout = Stdout.print(take(rbase.mk(3)))

import pa
import pb

# Both imports export `Cfg`, so this bare use is contested and must be
# refused HERE, in the file that wrote it. The root file gets exactly
# that error for the same shape; an imported module does not.
pub fn helper(c: Cfg) : Int = 1

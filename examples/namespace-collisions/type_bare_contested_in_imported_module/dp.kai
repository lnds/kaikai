import pa
import pb

# Both imports export `Cfg`, so this bare use is contested and is
# refused HERE, in the file that wrote it, exactly as the root file is
# for the same shape.
pub fn helper(c: Cfg) : Int = 1

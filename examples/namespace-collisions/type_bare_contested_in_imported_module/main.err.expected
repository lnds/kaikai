bare name 'Cfg' is exported by multiple modules: pa, pb
The contested bare `Cfg` is written in dp.kai's parameter type, so it is
refused there rather than surfacing at the call site in main.kai as a
mismatch blaming the caller for the callee's ambiguity.

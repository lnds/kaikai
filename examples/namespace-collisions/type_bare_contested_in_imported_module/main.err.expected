is exported by multiple modules
The contested bare `Cfg` is written in dp.kai and must be refused there,
exactly as the root file is for the same shape. Today it resolves
silently and surfaces as a type mismatch at the call site in main.kai,
blaming the caller for the callee's ambiguity.

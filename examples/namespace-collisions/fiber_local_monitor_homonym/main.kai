import watch
import core

fn main() = {
  let out = handle { "#{watch.watch("w")} #{core.observed()}" } with watch.Monitor {
    monitor(name, resume) -> resume("user-monitor:#{name}")
    return(x) -> x
  }
  Stdout.print(out)
}

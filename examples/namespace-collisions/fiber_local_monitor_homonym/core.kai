import actor

fn worker() : Unit / Actor[Nothing] + Cancel = Cancel.raise()

pub fn observed() : String / Spawn + Cancel + Monitor = with_mailbox {
  let pid = spawn_actor(worker)
  let _ = Monitor.monitor(pid)
  let _ = Actor.receive()
  "worker-down"
}

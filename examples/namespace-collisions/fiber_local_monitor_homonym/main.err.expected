type mismatch in op call Monitor.monitor
Today `Monitor.monitor` inside watch.kai binds to the core Monitor's op
although watch declares its own Monitor; main.out.expected holds the
target behaviour.

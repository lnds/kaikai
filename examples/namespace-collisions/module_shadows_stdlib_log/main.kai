# A local module outranks the stdlib module of the same name. Resolving the
# qualifier against the stdlib first makes the local file unreachable, and
# the diagnostic then offers the stdlib's exports for a name the user never
# imported.
import Log

fn main() : Unit / Stdout = Stdout.print(Log.show(Log.mk(3)))

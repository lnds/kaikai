# The shadowed declaration carrying the default is a user module rather
# than the prelude: the rule is about which declaration answers, not
# about the prelude in particular.
import a

effect Ev {
  fire(s: String) : Int
}

fn work() : Int / Ev = Ev.fire("hola")

fn main() : Int / Ev = work()

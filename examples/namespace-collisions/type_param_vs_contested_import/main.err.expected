a type parameter is not a contested import
The binder `[T]` settles `T` by rung 1 of the ladder; today the compiler
reports `bare name 'T' is exported by multiple modules: pa, pb` and
advises `pa.T`, which the user cannot write for their own parameter.

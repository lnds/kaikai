import xa
import xb

# One C symbol, two incompatible kaikai signatures: a conflict the
# compiler owns, not one to leave for `cc` (or, per translation unit,
# for nobody).
fn main() : Unit / Stdout + Ffi = Stdout.print("#{xa.up(97)} #{xb.up(97.0)}")

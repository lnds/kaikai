import acl

# `admin` is unique, `read` is not: the join is still ambiguous.
fn needs(h: FileHandle<read + admin>) : String = acl.label(h)

fn main() : Unit / Stdout = Stdout.print(needs(acl.issue()))

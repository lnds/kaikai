import alpha
import beta
import gamma

fn main() : Unit / Stdout = {
  let b = beta.Node("aa", "bb")
  let a = alpha.Node(7)
  Stdout.print("#{gamma.show_b(b)} #{gamma.show_a(a)}")
  Stdout.print("#{gamma.show_a(alpha.Node(1))} #{gamma.show_b(beta.Node("c", "d"))}")
}

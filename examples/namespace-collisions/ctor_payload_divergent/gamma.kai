import alpha
import beta

pub fn show_a(b: alpha.AlphaBox) : String = match b { Node(n, m) -> "alpha-int:#{n + m}" }
pub fn show_b(b: beta.BetaBox) : String = match b { Node(x, y) -> "beta-str:#{x}/#{y}" }

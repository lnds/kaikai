# Second arrangement of the call-forms cell: the bare call sits inside a
# lambda nested in another lambda. Depth must not change which `one` the
# lifted body reaches.
fn one(n: Int) : Int = n + 100

pub fn nested_lambda(xs: [Int]) : [[Int]] = xs | (x) => ([x] | (y) => one(y))

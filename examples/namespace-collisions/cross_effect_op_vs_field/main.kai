effect Store {
  get() : Int
}

type Bag = { get: String }

fn main() : Unit / Stdout = {
  let b = Bag { get: "field" }
  let n = handle { Store.get() } with Store { get(resume) -> resume(7) }
  Stdout.print("#{b.get} #{n}")
}

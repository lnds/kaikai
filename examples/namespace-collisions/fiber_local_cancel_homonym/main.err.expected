no member named 'abort' in 'struct Evconcurrent__Cancel'
Today the qualified head `with guard.Cancel` is emitted against the core
Cancel's evidence struct and the pushed label is the bare "Cancel", so the
C build fails; main.out.expected holds the target behaviour.

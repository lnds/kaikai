import sp

# The module's private `read_line` answers its own calls; the root file
# declares nothing under that name, so the builtin keeps every other
# spelling here.
fn main() : Unit / Stdout = {
  Stdout.print(describe("a"))
  Stdout.print(describe(""))
  Stdout.print(int_to_string(string_length("abc")))
}

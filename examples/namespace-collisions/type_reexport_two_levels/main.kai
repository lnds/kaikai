import lvl0
import lvl1
import lvl2

# Two hops instead of one. The home is assigned where the type is
# declared, so it survives however many modules pass it along.
fn take(c: Cfg) : String = "#{lvl1.hop(c)} #{lvl2.hop2(c)}"

fn main() : Unit / Stdout = Stdout.print(take(lvl0.mk(4)))

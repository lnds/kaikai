import ua
import ub
import uc
import ud

# `ua` and `ud` are named by qualified calls, `uc` only by the bare
# protocol operation `render`; `ub` is the one dead import.
fn main() : Unit / Stdout = Stdout.print("#{ua.greet()} #{render(ud.mk(3))}")

import pa
import pb

# The override collides on arity rather than on parameter type: the
# same symbol cannot take one argument in one module and two in another.
fn main() : Unit / Stdout + Ffi = Stdout.print("#{pa.a(97)} #{pb.b(97)}")

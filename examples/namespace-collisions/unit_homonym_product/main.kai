import ua
import ub

# Two homonymous units are two units: their product and quotient keep
# both homes apart.
fn main() : Unit / Stdout = {
  let p = ua.ua(2.0) * ub.ub(3.0)
  let q = ua.ua(2.0) / ub.ub(4.0)
  Stdout.print("#{p} #{q}")
}

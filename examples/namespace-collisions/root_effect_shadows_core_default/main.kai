# A root effect named like a prelude effect that carries a `default { }`.
# The prelude handler serves the prelude declaration's ops, not these, so
# the label must not be absorbed: an unhandled use is an error.
effect Log {
  note(s: String) : Unit
}

fn work() : Unit / Log = Log.note("hola")

fn main() : Unit / Log = work()

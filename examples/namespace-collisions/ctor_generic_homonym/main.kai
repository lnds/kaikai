import ga
import gb

fn main() : Unit / Stdout = {
  let p = gb.Wrap(1, 2)
  let h = ga.Wrap("s")
  let here = match p { gb.Wrap(x, y) -> "main-pair:#{x},#{y}" }
  let there = match h { ga.Wrap(v) -> "main-holder:#{v}" }
  Stdout.print("#{gb.fst(p)} #{ga.get(h)} #{here} #{there}")
}

import ga
import gb
import gc

fn main() : Unit / Stdout = {
  let p = gb.Wrap(1, 2)
  let h = ga.Wrap("s")
  Stdout.print("#{gc.fst(p)} #{gc.snd(p)} #{gc.get(h)} #{gc.get(ga.Wrap(true))} #{gc.snd(gb.Wrap("x", "y"))}")
}

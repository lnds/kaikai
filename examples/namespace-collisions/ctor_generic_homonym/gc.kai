import ga
import gb

pub fn get[a](h: ga.Holder[a]) : a = match h { Wrap(x) -> x }
pub fn fst[a](p: gb.Pair[a]) : a = match p { Wrap(x, _) -> x }
pub fn snd[a](p: gb.Pair[a]) : a = match p { Wrap(_, y) -> y }

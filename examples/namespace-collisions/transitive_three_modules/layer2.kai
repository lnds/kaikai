import layer1

pub type Err = Failure(Int)

pub fn mk(n: Int) : Err = Failure(n)
pub fn show(e: Err) : String = match e { Failure(n) -> "L2:#{n}" }
pub fn via1() : String = layer1.show(layer1.mk("x"))

# A generic fn in the root file whose name collides with a generic core fn.
# The root file carries no module tag, which is what makes this arrangement
# distinct from the module-qualified one.
fn count[T](s: String, p: T) : Int = string.length(s)

fn main() : Unit / Stdout = Stdout.print("#{count("hola", 0)}")

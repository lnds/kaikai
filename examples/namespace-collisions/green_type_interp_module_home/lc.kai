import la
import lb

# The interpolation here is read from this module's viewpoint, not the
# root's: its import table, not the root file's, decides which home a
# qualified `m.Cfg` picks.
pub fn tag() : String = "in-lc:#{la.read(la.Cfg { a: 3 })}|#{lb.read(lb.Cfg { b: 4 })}"

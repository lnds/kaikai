import la
import lb
import lc

# Same respelling as green_type_interp, but the fragment lives in an
# imported module's fn, so the home-spelling context is that module's.
fn main() : Unit / Stdout =
  Stdout.print("#{lc.tag()} #{la.read(la.Cfg { a: 1 })} #{lb.read(lb.Cfg { b: 2 })}")

pub type Chain = Leaf | Link(Chain)

pub fn len(c: Chain) : Int = match c {
  Leaf -> 0
  Link(n) -> 1 + len(n)
}

pub fn grow(c: Chain) : Chain = match c {
  Leaf    -> Link(Leaf)
  Link(n) -> Link(Link(n))
}

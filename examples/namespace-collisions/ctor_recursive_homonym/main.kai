import ra
import rb
import rc

fn main() : Unit / Stdout = {
  let chain = rb.Link(rb.Link(rb.Leaf))
  let tree = ra.Node2(ra.Leaf, ra.Node2(ra.Leaf, ra.Leaf))
  Stdout.print("#{rc.len(chain)} #{rc.depth(tree)} #{rc.len(rb.grow(chain))} #{rc.depth(ra.Leaf)} #{rc.len(rb.Leaf)}")
}

pub protocol Seeded {
  seed() : Self
  bump(x: Self) : Self
}

pub type Counter = Counter(Int)

impl Seeded for Counter {
  fn seed() : Counter = Counter(0)
  fn bump(x: Counter) : Counter = match x { Counter(n) -> Counter(n + 1) }
}

import watch
import core

fn main() = {
  let out = handle { "#{watch.watch("w")} #{core.observed()}" } with Watch {
    monitor(name, resume) -> resume("user-monitor:#{name}")
    return(x) -> x
  }
  Stdout.print(out)
}

bare name 'Cfg' is exported by multiple modules: pa, pb
dp.kai does not resolve on its own: the contested bare `Cfg` it writes
is refused wherever compilation reaches it. The corpus drives it through
main.kai, whose DIAG.expected demands the diagnostic name dp.kai as the
file that owns the position.

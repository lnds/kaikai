import ua
import ub

# There is no module qualifier for a habitant: `Kind.habitant` is the
# only qualified form, and it cannot separate two units of one kind.
fn twice(x: Real<ua.tick>) : Real<ua.tick> = x * 2.0

fn main() : Unit / Stdout = Stdout.print("#{twice(ua.ua(1.0))}")

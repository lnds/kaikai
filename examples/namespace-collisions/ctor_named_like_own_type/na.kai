# The constructor is named like its OWN type, so `na.Alpha` is both the
# type and the constructor inside one home. The neighbouring module spells
# the same name for a record type.
pub type Alpha = Alpha(Int) | Blank

pub fn show(a: Alpha) : String = match a { Alpha(n) -> "own:#{n}"  Blank -> "own:blank" }

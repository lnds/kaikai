import pa

# `Hidden` is a private transparent alias. A bare reference from another
# module is a privacy violation, not a missing name: the alias is spelled
# to its home at birth, so only the display spelling reaches this use.
fn wrap(x: Hidden) : Int = x

fn main() : Unit / Stdout = Stdout.print("#{wrap(pa.mk())}")

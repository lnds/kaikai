import sa
import sb

# A nullary homonym has no argument list, so the help text cannot show
# `sa.Ready(...)` the way it does for a payload-carrying one. The advice
# still has to name a spelling the reader can type.
fn main() : Unit / Stdout = {
  let s = Ready
  Stdout.print("x")
}

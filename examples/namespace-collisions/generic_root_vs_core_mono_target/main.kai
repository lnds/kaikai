# Control: a generic root fn named like a monomorphic core fn, so only one
# side of the collision is generic.
fn contains[T](s: String, p: T) : Int = string.length(s)

fn main() : Unit / Stdout = Stdout.print("#{contains("hola", 0)}")

import ta
import tb
import tc

# Three homes for the effect and the bare name in an OP CALL. The
# candidate list has to name all three: a reader sent to the first pair
# picks a qualifier that is still ambiguous with the third.
fn go() : Unit = Track.note("x")

fn main() : Unit / Stdout = Stdout.print("y")

import alpha
import beta

# The homonyms are NULLARY, so there is no argument whose type could push
# the resolver toward one home. A bare `Ready` is ambiguous with nothing
# left to break the tie.
fn main() : Unit / Stdout = {
  let s = Ready
  Stdout.print("x")
}

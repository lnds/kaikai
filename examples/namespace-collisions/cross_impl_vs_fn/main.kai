protocol Sized {
  size(self: Int) : Int
}

impl Sized for Int {
  fn size(self: Int) : Int = self * 2
}

fn size(s: String) : Int = 99

fn main() : Unit / Stdout = Stdout.print("#{4.size()} #{size("x")}")

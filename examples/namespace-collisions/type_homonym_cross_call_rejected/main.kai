import ma
import mb
import mc

# ma.Mark and mb.Mark are distinct nominal types that happen to share a
# name and a shape; one does not flow where the other is expected.
fn main() : Unit / Stdout = Stdout.print("#{mc.first(mb.Tok(1, 2))}")

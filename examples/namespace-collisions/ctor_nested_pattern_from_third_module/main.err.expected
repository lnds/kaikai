ambiguous constructor 'Tok': declared by modules [mb, ma] and the use site names no home
Today only an arm's top-level constructor takes the scrutinee's home, so
the nested spelling panics the emitter; main.out.expected holds the target
behaviour.

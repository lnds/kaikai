import sb

# The same collision where the core's function takes an argument and the
# import's takes none. The disagreement was not silent here: the native
# backend rejected the call it emitted with an arity mismatch.
fn main() : Unit / Stdout = Stdout.print("v=#{abs_sq()}")

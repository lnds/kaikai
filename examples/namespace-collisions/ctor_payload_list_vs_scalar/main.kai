import qa
import qb

fn main() : Unit / Stdout = {
  let l = qb.Item([1, 2])
  let n = qa.Item(4)
  let here = match l { qb.Item(xs) -> "main-len:#{xs.length()}" }
  let there = match n { qa.Item(k) -> "main-n:#{k}" }
  Stdout.print("#{qb.lb_s(l)} #{qa.la_s(n)} #{here} #{there}")
}

import pa
import pb

fn main() : Unit / Stdout = {
  let r = pb.Box(pb.Dim { w: 2, h: 3 })
  let i = pa.Box(7)
  let here = match r { pb.Box(d) -> "main-rec:#{d.w}x#{d.h}" }
  let there = match i { pa.Box(n) -> "main-int:#{n}" }
  Stdout.print("#{pb.rb_s(r)} #{pa.ra_s(i)} #{here} #{there}")
}

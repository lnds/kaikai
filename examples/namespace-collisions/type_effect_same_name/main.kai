type Log = { level: Int }

effect Log {
  write(s: String) : Unit
}

fn emit() : Unit / Log = Log.write("hello")

fn main() : Unit / Stdout = {
  let cfg = Log { level: 3 }
  handle { emit() } with Log { write(s, resume) -> resume(Stdout.print("#{s}/#{cfg.level}")) }
}

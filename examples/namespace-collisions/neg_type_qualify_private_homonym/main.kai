import qa
import qb

# `qa.Cfg` is private and `qb.Cfg` is public. A visible homonym must not
# stand in for the private one the qualifier actually named.
fn look(c: qa.Cfg) : Int = c.hidden

fn main() : Unit / Stdout = Stdout.print("#{qa.peek()} #{qb.mk().open}")

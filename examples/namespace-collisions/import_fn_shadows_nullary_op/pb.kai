pub protocol Scalable {
  unit_of() : Self
  scale_by(x: Self, k: Int) : Self
}

pub type Meters = Meters(Int)

impl Scalable for Meters {
  fn unit_of() : Meters = Meters(1)
  fn scale_by(x: Meters, k: Int) : Meters = match x { Meters(n) -> Meters(n * k) }
}

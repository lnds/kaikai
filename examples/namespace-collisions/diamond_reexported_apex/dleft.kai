import dbase

pub type Err = Failure(Int) | Clean

# The left branch hands the apex's type back out under its own name.
pub fn lift(m: String) : dbase.Err = dbase.Failure(m)
pub fn own(n: Int) : Err = Failure(n)

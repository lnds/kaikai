import hd

fn main() : Unit / Stdout = {
  let outer = handle {
    let inner_v = handle { hd.inner() } with Deep { get(resume) -> resume(1) }
    inner_v + hd.inner()
  } with Deep { get(resume) -> resume(10) }
  Stdout.print("#{outer}")
}

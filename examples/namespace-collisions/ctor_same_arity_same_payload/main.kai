import pa
import pb
import pc

fn main() : Unit / Stdout = {
  let m = pa.Tok(1, 2)
  let s = pb.Tok(1, 2)
  Stdout.print("#{pc.first(m)} #{pc.second(s)} #{pc.tally([m, pa.Tok(3, 4)], 0)} #{pc.first(pa.Tok(9, 0))} #{pc.second(pb.Tok(0, 9))}")
}

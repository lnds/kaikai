import spawn
import oa
import ob

fn main() : Unit / Stdout + Spawn = {
  nursery { n ->
    let f = n.spawn(() => {
      Stdout.print(oa.sa_show(oa.Msg(1)))
    })
    n.await(f)
  }
  Stdout.print(ob.sb_show(ob.Msg("x")))
}

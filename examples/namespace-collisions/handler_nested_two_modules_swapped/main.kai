import ea
import eb

fn main() : Unit / Stdout = {
  handle {
    handle {
      eb.run()
      ea.run()
    } with ea.Emit { put(s, resume) -> resume(Stdout.print("inner-a:#{s}")) }
    eb.run()
  } with eb.Emit { put(s, resume) -> resume(Stdout.print("outer-b:#{s}")) }
}

import lib as l
import helper

# The alias is reported under its written name, and a dead import inside
# an imported module is reported against that module's file.
fn main() : Unit / Stdout = Stdout.print(helper.shout("hi"))

# Two same-named constructors with different payloads must occupy two
# distinct table entries. Today the emit table deduplicates by bare
# name, so only one "Node" reaches the generated C and both modules
# inherit the first one's arity and payload layout.
"alpha::Node"
"beta::Node"

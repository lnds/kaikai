import ga
import gb
import gc

fn main() : Unit / Stdout = {
  let p = gb.Wrap(1, 2)
  let h = ga.Wrap("k", "s")
  Stdout.print("#{gc.fst(p)} #{gc.snd(p)} #{gc.key(h)} #{gc.get(h)} #{gc.get(ga.Wrap("t", true))} #{gc.snd(gb.Wrap("x", "y"))}")
}

# The same gate on a triple-quoted literal, with a good fragment before
# the bad one: the scan must reach every part, not stop at the first.
fn main() : Unit / Stdout = {
  let n = 1
  Stdout.print("""a=#{n} b=#{ n * }""")
}

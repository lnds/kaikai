import money

# `money.USD` is in scope here, and shop declares its own; the bare
# spelling is shop's — the current module's declaration wins.
pub currency USD

pub fn price() : Real<USD> = 5.0<USD>
pub fn total(n: Real) : Real<USD> = price() * n

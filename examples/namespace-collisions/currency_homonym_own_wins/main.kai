import money
import shop

# `shop` declares its own USD and uses it bare; the stdlib one being in
# the program does not reach into shop's scope.
fn main() : Unit / Stdout = Stdout.print("#{shop.price() * 2.0}")

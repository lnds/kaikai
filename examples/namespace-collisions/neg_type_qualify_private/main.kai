import qa

# `Secret` is declared without `pub`, so it is not part of qa's surface.
# Naming it through the qualifier must fail the same way an absent type
# does — privacy is not a weaker form of existing.
fn look(s: qa.Secret) : Int = s.hidden

fn main() : Unit / Stdout = Stdout.print("#{qa.peek()}")

import ma
import mb

fn main() : Unit / Stdout = {
  let t = ma.Fork(ma.Leaf(1), ma.Fork(ma.Leaf(2), ma.Leaf(3)))
  let u = mb.Fork(mb.Fork(mb.Leaf("a"), ma.Leaf(9)), t)
  Stdout.print("#{ma.sum(t)} #{mb.render(u)}")
}

import ma

# The homonyms nest the OTHER module's recursive type inside this one's,
# so each recursive step alternates homes.
pub type Tree = Leaf(String) | Fork(Tree, ma.Tree)

pub fn render(t: Tree) : String = match t {
  Leaf(s)    -> s
  Fork(l, r) -> "(#{render(l)}|#{ma.sum(r)})"
}

import ra
import rb

fn main() : Unit / Stdout = {
  let s = rb.Flag("on")
  let b = ra.Flag(true)
  let here = match s { rb.Flag(v) -> "main-s:#{v}" }
  let there = match b { ra.Flag(v) -> "main-b:#{v}" }
  Stdout.print("#{rb.bb_s(s)} #{ra.ba_s(b)} #{here} #{there}")
}

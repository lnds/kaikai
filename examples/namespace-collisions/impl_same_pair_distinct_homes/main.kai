# The pair (Render, Point) is spelled identically in both modules; each
# impl must dispatch to its own home.
import ia
import ib

fn main() : Unit / Stdout = {
  let b = ib.Point { y: 2 }
  let a = ia.Point { x: 1 }
  Stdout.print("#{b.render()} #{a.render()}")
}

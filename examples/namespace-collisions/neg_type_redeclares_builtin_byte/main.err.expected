type 'Byte' is a reserved builtin type name and cannot be redeclared
A fixed-width primitive is reserved on the same footing as Int, so the
whole primitive set is refused at the declaration rather than only the
few names a hand-written list happened to carry.

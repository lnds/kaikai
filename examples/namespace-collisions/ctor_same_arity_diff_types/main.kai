import wa
import wb

fn main() : Unit / Stdout = {
  let s = wb.Cell("z")
  let i = wa.Cell(4)
  let here = match s { wb.Cell(v) -> "main-str:#{v}" }
  let there = match i { wa.Cell(v) -> "main-int:#{v}" }
  Stdout.print("#{wb.s2(s)} #{wa.s1(i)} #{here} #{there}")
}

# `reverse` is declared by both auto-loaded core modules, list and
# string. The lambda's parameter is never applied, so its type never
# settles and names no candidate. The report names both and the
# qualified form that resolves it.
fn main() : Unit / Stdout = {
  let _f = (x) => reverse(x)
  Stdout.print("unreachable")
}

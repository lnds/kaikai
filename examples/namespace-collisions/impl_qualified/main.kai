import pa
import pb

type Box = { n: Int }

# Two modules declare `Render`. The qualifier on the impl head says
# which protocol this impl supplies.
impl pa.Render for Box { fn render(x: Box) : String = "pa-render" }

fn main() : Unit / Stdout = Stdout.print(render(Box { n: 1 }))

# The core-qualified spelling is rejected for the same reason as the
# bogus one: no home slot exists for this pattern shape.
fn main() : Unit / Stdout = {
  let p = (1, 2)
  let got = match p {
    tuple.Pair { fst: a, snd: b } -> a + b
  }
  Stdout.print(int_to_string(got))
}

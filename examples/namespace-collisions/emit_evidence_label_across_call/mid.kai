import ea

# Only ONE of the two homes is in scope here, so this module's row can
# name `Ask` bare. The evidence its perform finds is whatever the caller
# pushed, and the caller has both homes live.
pub fn both() : Int / Ask = ea.get()

protocol Emitter {
  emit(self: Int) : String
}

effect Emit {
  emit(s: String) : Unit
}

impl Emitter for Int {
  fn emit(self: Int) : String = "proto-#{self}"
}

fn main() : Unit / Stdout = {
  handle { Emit.emit(3.emit()) } with Emit { emit(s, resume) -> resume(Stdout.print(s)) }
}

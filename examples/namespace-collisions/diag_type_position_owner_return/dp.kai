import pa
import pb

# The contested bare name is the RETURN type here rather than a
# parameter. Either position is written in this file, which therefore
# owns the position the diagnostic carries.
pub fn build() : Cfg = pa.Cfg { v: 2 }

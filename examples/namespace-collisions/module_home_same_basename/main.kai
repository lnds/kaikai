import a.util
import b.util

# Two modules with one basename mint one home: the second file's symbols
# collide with the first at link time. The home belongs to the module's
# package-relative path, not to its basename.
fn main() : Unit / Stdout = Stdout.print("#{util.who()}")

pub protocol Render { render(x: Self) : String }

pub type Box = Box(Int)

impl Render for Box {
  fn render(x: Box) : String = match x { Box(n) -> "b#{n}" }
}

pub fn show_it() : String = render(Box(2))

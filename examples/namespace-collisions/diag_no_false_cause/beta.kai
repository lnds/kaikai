pub type BetaError = RepositoryFailure(String)

pub fn beta_make(m: String) : BetaError = RepositoryFailure(m)

pub fn beta_show(e: BetaError) : String = match e {
  RepositoryFailure(m) -> "beta:#{m}"
}

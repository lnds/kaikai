# A fragment the interp parser rejects must fail the build. Every
# consumer re-parses the span and substitutes the empty string on
# failure, so without the gate this compiles and prints `v=`.
fn main() : Unit / Stdout = Stdout.print("v=#{ 1 + + }")

import acl

# `acl` declares its own `read`; the stdlib `read` in the program does
# not make acl's bare use ambiguous.
fn main() : Unit / Stdout = Stdout.print("#{acl.label(acl.issue())} #{acl.label_admin(acl.issue())}")

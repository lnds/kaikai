# The contested call is written in a module body rather than the root
# file, and both candidates arrive through this module's own imports.
import qa
import qb

pub fn describe(r: Result[[Int], String]) : Result[String, String] =
  r.map(xs => tag(xs))

import encoding.json.{json_decode}

#[derive(Json)]
pub type Item = { name: String }

pub fn parse(s: String) : String = match json_decode(s) {
  None -> "bad"
  Some(v) -> match item_of_json(v, "") {
    Ok(i) -> "name=#{i.name}"
    Err(_) -> "err"
  }
}

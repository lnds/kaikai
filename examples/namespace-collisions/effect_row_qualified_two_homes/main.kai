import qa
import qb
import mid

fn main() : Unit / Stdout = {
  handle {
    handle { mid.both() } with qa.Emit { put(s, resume) -> resume(Stdout.print("qa:#{s}")) }
  } with qb.Emit { put(n, resume) -> resume(Stdout.print("qb:#{n}")) }
}

protocol Named {
  label(self: Int) : String
}

type Named = { tag: String }

impl Named for Int {
  fn label(self: Int) : String = "int-#{self}"
}

fn main() : Unit / Stdout = {
  let r = Named { tag: "rec" }
  Stdout.print("#{5.label()} #{r.tag}")
}

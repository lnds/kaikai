# A const's type annotation names a type that two modules both declare,
# and each module reads its OWN const by bare name. The annotation must
# resolve to the declaring module's record, or the bare read reaches the
# wrong one.

import ka
import kb

fn main() : Unit / Stdout = Stdout.print("#{ka.ka_cap()} #{kb.kb_name()}")

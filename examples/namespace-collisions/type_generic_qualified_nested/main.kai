import ga
import gb

# One home's generic instantiated at the other home's generic: the
# argument position must keep its own home while the head keeps its.
fn nest(b: ga.Box[gb.Box[Int]]) : Int = gb.get(b.item)

fn main() : Unit / Stdout = Stdout.print("#{nest(ga.Box { item: gb.wrap(5) })}")

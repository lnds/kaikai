# Three modules export `score`, and the lambda's parameter is never
# applied, so nothing settles its type. The report names all three.
import va
import vb
import vc

fn main() : Unit / Stdout = {
  let _f = (x) => score(x)
  Stdout.print("unreachable")
}

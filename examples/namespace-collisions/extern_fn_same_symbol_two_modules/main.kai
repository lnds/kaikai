import xa
import xb

# Two modules binding the same C symbol with agreeing signatures is the
# ordinary pattern and stays legal.
fn main() : Unit / Stdout + Ffi = Stdout.print("#{xa.ca(0.0)} #{xb.cb(0.0)}")

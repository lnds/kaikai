# The tie of `neg_import_order_cannot_decide` reached through aliases
# rather than plain imports. The bare call is contested exactly as
# before, and the candidates are spelled with the aliases the reader
# actually has in scope — the qualified form the help suggests has to be
# one this file can type.
import ta as first
import tb as second

fn main() : Unit / Stdout = Stdout.print(render())

import oa
import ob

# The kaikai spellings differ; the C symbol they override to does not.
# Identity at the boundary is the symbol, so this is the same conflict
# two matching spellings would raise.
fn main() : Unit / Stdout + Ffi = Stdout.print("#{oa.a(65)} #{ob.b(65.0)}")

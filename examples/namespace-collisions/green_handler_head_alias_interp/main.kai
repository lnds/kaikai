import hb as clock

# The qualifier is an import ALIAS, and the head carries a `return`
# clause: the fragment resolves the alias against its own file's table.
fn main() : Unit / Stdout =
  Stdout.print("r=#{handle { clock.run(4) } with clock.Tick { tick(n, resume) -> resume(n * 3) } return(v) -> v}")

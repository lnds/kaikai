expected `fn`, `type`, `effect`, `protocol`, `impl`, `test`, `unit`, `use`, `axiom`, `const`, or `import`
The qualified union member does not parse today; that is the defect.
main.out.expected is what the fixture must print once it does, so the
`c` axis stays red until the parser accepts `m.T` in a union.

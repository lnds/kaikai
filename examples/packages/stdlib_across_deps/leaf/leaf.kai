# `leaf` uses stdlib `string_concat` (prelude builtin) and
# `string_length` to build a labelled message.
pub fn label(name: String) : String =
  string_concat("leaf<", string_concat(name,
    string_concat(">[", string_concat(int_to_string(string_length(name)), "]"))))

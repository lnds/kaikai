import leaf

# `mid` uses stdlib (list operations + leaf's API).
pub fn labels(names: [String]) : [String] =
  list_map(names, (n) => leaf.label(n))

fn list_map(xs: [String], f: (String) -> String) : [String] = match xs {
  []           -> []
  [h, ...rest] -> [f(h), ...list_map(rest, f)]
}

name = "consumer"
version = "0.1.0"
edition = "hanga-roa"

[dependencies]
mid = { path = "../mid" }
# `leaf` is mid's dep, not consumer's logical dep — we list it
# here only because kai-pkg paths v1 does not walk transitive
# local-path deps yet. Remove once the driver supports transitive
# resolution (see issue body §coverage).
leaf = { path = "../leaf" }

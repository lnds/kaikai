# examples/packages/transitive — `util` depends on `greet`, both
# resolved transitively through `kai.toml`. Spec: docs/packages.md.
#
# Setup:
#   tests/fixtures/git-fixtures/setup.sh
#   examples/packages/render-fixtures.sh
#   bin/kai install   # in this directory
#   bin/kai run examples/packages/transitive/main.kai

import util

fn main() {
  print(util.shout("transitive"))
}

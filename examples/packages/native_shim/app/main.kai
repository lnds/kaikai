# Consumer of a shim-binding dependency. No Makefile, no CFLAGS:
# the dep's [native] table is the whole contract.

import term

fn main() : Unit / Stdout + Ffi = {
  Stdout.print("#{term.cols()}")
}

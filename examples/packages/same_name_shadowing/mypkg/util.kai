# Declare a `length` function that shadows nothing in this module's
# scope — but a stdlib `list.length` exists. The point of this
# fixture is to confirm the package's own qualified name wins under
# its own dotted path, while bare stdlib calls keep working.

pub fn length(label: String) : String =
  string_concat("len:", label)

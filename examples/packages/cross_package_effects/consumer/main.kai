import mid
import leaf

fn main() {
  let total = handle { mid.tick_three() } with Counter {
    var n = 0
    bump(resume) -> {
      n := @n + 1
      resume(@n)
    }
  }
  print(int_to_string(total))
}

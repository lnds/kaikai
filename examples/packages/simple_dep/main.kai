# examples/packages/simple_dep — single git-source dependency
# resolved through `kai.toml`. Spec: docs/packages.md (issue #405).
#
# Build the bare repo first:
#   tests/fixtures/git-fixtures/setup.sh
# Render the manifest from the template (paths are absolute):
#   examples/packages/render-fixtures.sh
# Then:
#   bin/kai run examples/packages/simple_dep/main.kai

import greet

fn main() {
  print(greet.greet("simple_dep"))
}

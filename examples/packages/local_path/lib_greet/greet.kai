# A trivial library imported by examples/packages/local_path/app/main.kai
# via a `path = "../lib_greet"` dependency in app/kai.toml.

pub fn greet(name: String) : String =
  string_concat("Hello, ", string_concat(name, "!"))

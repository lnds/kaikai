import shadowpkg.lib

# The module's own `read_line` runs; the root file declares nothing under
# that name, so `string_join` here is still the builtin.
fn main() {
  print(string_join(lib.describe(["a", "", "b"]), ","))
}

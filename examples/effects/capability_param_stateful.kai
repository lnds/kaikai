effect Res {
  acquire(tag: String) : Int
}

fn take(r: Res) : Int = r.acquire("boxed-tag")

fn main() : Unit / Stdout = {
  let n = handle { take(r) } with Res(6) as r {
    acquire(tag, resume) -> { Stdout.print("tag=#{tag}") resume(state) }
  }
  Stdout.print("n=#{n}")
}

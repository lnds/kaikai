# issue #624 regression guard (variant 3): inline function-type
# annotation with a concrete row, no alias involved. This path
# never paniced; the fixture keeps it covered so future row-handling
# changes do not regress it.

fn main() : Int = {
  let f : () -> Option[Int] / Stdout = () => None
  let _ = f
  print("alias_inline_row ok")
  0
}

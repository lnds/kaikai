# m7b #12 positive — a row-polymorphic helper called with a pure
# body has its row variable instantiated to the empty row, so the
# caller stays pure.

fn twice_int(body: () -> Int / e) : Int / e =
  body() + body()

fn main() : Int {
  assert twice_int(() => 42) == 84
  0
}

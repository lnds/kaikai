# m7e §24 — main-row inference. `main` declares no row; the typer
# infers Stdout from the body and the runtime default-handler
# wrapper installs it automatically.

fn main() = {
  Stdout.print("hello, inferred row")
  0
}

# Case B — the reference shape the nursery re-raise must match: a
# direct `Cancel.raise()` under a user handler runs the clause and
# resumes after the handle.

fn main() : Unit / Stdout + Cancel {
  handle {
    Cancel.raise()
  } with Cancel {
    raise(resume) -> println("main caught the direct raise")
  }
  println("main continues")
}

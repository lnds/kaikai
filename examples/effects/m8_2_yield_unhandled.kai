# m8 #2 negative test — `Spawn.yield()` is called from a function
# whose row does not include `Spawn`. The typer must reject and the
# diagnostic must follow the m7a #8 shape (error + note + help) per
# Doc C §*Diagnostic quality* §*Effect not handled*.

fn main() : Int / Console {
  Spawn.yield()
  0
}

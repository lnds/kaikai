# m7b #1 — single-effect alias. `type Foo = Console` is a one-name
# shorthand: `/ Foo` resolves to `/ Console`, and the runtime installs
# the default Console handler around `main` accordingly.

type Foo = Console

fn main() : Int / Foo {
  Stdout.print("single-alias works")
  0
}

# Issue #533: zero-clause `default { }` is legal — degenerate case
# equivalent to declaring no default at all. Parser smoke only.

effect Tick {
  tick() : Unit
  default {
  }
}

fn main() : Int {
  let r = handle {
    Tick.tick()
    3
  } with Tick {
    tick(resume) -> resume(())
  }
  r
}

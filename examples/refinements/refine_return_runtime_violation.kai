# A dynamic return value that violates the refined return type panics
# at the return with the structured refinement diagnostic.
type NonNeg = Int where self >= 0

fn shrink(x: Int) : NonNeg = x - 10

fn main() : Unit / Stdout = Stdout.print(int_to_string(shrink(3)))

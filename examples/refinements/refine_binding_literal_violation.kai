# A literal binding into a refined alias must be rejected at compile
# time: the closed value 1.5 refutes `0.0 <= self and self <= 1.0`.
type Percent = Real where 0.0 <= self and self <= 1.0

fn main() : Unit / Stdout {
  let bad : Percent = 1.5
  Stdout.print(real_to_string(bad))
}

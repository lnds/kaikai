# A closed return value that refutes the declared refined return type
# must be rejected at compile time.
type NonNeg = Int where self >= 0

fn make() : NonNeg = 0 - 3

fn main() : Unit / Stdout = Stdout.print(int_to_string(make()))

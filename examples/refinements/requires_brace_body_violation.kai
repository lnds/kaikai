# A `requires` clause stays in force when the body is a brace block, not
# just when it is `= expr`: the literal call below must be rejected.
fn b(n: Int) : Int requires n > 0 { n + 1 }

fn main() : Unit / Stdout = Stdout.print("#{b(0)}")

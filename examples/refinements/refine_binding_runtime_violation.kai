# A dynamic value bound under a refined annotation panics at the
# binding with the structured refinement diagnostic.
type NonNeg = Int where self >= 0

fn produce(x: Int) : Int = x - 10

fn main() : Unit / Stdout {
  let bad : NonNeg = produce(3)
  Stdout.print(int_to_string(bad))
}

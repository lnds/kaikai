# Negative (#1127): `^` is not valid on a lambda parameter — lambda params
# are local and inference covers them. Expect a parse error.
fn main() : Int / Stdout = {
  let f = (^x: Int) => x + 1
  Stdout.print(int_to_string(f(5)))
  0
}

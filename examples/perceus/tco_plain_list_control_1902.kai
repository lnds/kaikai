# Control: the same TCO loop over a bare list, no record or variant wrapper.
fn build(e: [String], i: Int, n: Int) : [String] =
  if i >= n { e } else { build(["p", ...e], i + 1, n) }

fn main() : Unit / Stdout = {
  let e = build([], 0, 200000)
  Stdout.print("n=#{list_length(e)}")
}

# Negative (#1127): `^` outside parameter position is not an expression
# form (power is infix-only). Expect a parse error.
fn main() : Int / Stdout = {
  let x = ^5
  Stdout.print(int_to_string(x))
  0
}

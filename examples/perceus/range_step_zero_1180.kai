# Issue #1180 — a zero step is a runtime trap (the generator would
# never terminate). Must trap identically whether the range is
# consumed or not.

fn main() : Unit / Stdout = {
  Stdout.print(int_to_string(length([1..10..0])))
}

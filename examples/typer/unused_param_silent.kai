# issue #381 — function parameters that go unused must NOT warn.
# Interface compliance, protocol dispatch, and contract-with-caller
# all justify the silence (the issue body's principle table).
fn pick_first(a: Int, b: Int) : Int = a

fn main() {
  let r = pick_first(1, 2)
  print(int_to_string(r))
}

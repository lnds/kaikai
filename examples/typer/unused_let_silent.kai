# issue #381 — `_`-prefixed binders are explicitly "intentionally
# unused" and must NOT trigger any diagnostic. `_x` and `__internal`
# both qualify; only the latter is the compiler-internal namespace.
fn main() {
  let _x = 42
  let __internal = 7
  print("ok")
}

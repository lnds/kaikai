# issue #381 — `let x = ...` whose `x` is never referenced should
# trigger a `warning: unused binding` diagnostic. Compilation must
# still succeed (warnings never fail the build).
fn main() {
  let x = 42
  print("hello")
}

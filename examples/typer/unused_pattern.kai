# issue #381 — pattern bindings whose binders are never referenced
# in the arm body should warn. Here `Some(v)` introduces `v`, but
# the body discards it.
fn main() {
  let r = match Some(7) {
    Some(v) -> "got it"
    None    -> "empty"
  }
  print(r)
}

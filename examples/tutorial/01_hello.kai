# Tutorial step 1 — hello, kaikai.
#
# `print` is a flat builtin. `main` declares no return type and no
# effect row; both are inferred. The runtime installs a default
# Stdout handler around `main` because the inferred row contains
# Stdout.

fn main() = {
  print("hello, kaikai")
}

# `vault.Lock` is private: an impl outside `vault` must be rejected
# naming the owning module — not silently validated against it.
import vault

type Chest = { id: Int }

impl Lock for Chest {
  fn key(x: Chest) : Int = x.id
}

fn main() : Unit / Stdout = {
  Stdout.print("#{vault.open_one()}")
}

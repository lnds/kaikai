# `tick` is exported by both imports and the root declares none of its
# own, so the bare reference cannot choose a module.
import metric
import imperial

fn main() : Unit / Stdout = {
  let x = 1.0<tick>
  Stdout.print("#{x}")
}

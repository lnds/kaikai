# An exported alias whose name another module also exports with a
# different body. Each module's `span` must expand to its own base.
pub unit m
pub unit span = m

pub fn span_m_mk() : Real<span> = 4.0<m>
pub fn span_m_ratio(x: Real<span>) : Real = x / 1.0<m>

# A private `tick` in a module that never imports the exported ones:
# its own declaration wins inside this module.
unit tick

fn width() : Real<tick> = 7.0<tick>

pub fn local_ratio() : Real = width() / 1.0<tick>

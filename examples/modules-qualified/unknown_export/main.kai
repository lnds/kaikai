# m6.2 negative fixture: imported module, missing export.
# rqc detects the situation directly (the module is in the table
# but `nope` is not in its exports) and emits a specific
# diagnostic that names the available exports. The compile must
# fail.

import arith

fn main() {
  print(int_to_string(arith.nope(41)))
}

# An exported protocol another module implements. `hud` declares a
# pub protocol of the same name with a different signature; `screen`
# must still resolve `Shade` to this one, the first visible
# declaration from its viewpoint.
pub protocol Shade {
  shade(x: Self) : String
}

# Same names as alpha, declared later in the stream. The interp
# bodies below must read THIS module's consts, bare and qualified,
# uppercase and lowercase.
pub const MAX : Int = 200
pub const rate : Int = 22

pub fn beta_bare_max() : String = "#{MAX}"
pub fn beta_bare_rate() : String = "#{rate}"

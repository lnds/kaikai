# A private alias whose name a sibling module also declares. It must
# stay inside this module: nothing here is exported but `depth`.
type Step = (Int, Int)

fn advance() : Step = (2, 3)

pub fn depth() : Int = advance().fst

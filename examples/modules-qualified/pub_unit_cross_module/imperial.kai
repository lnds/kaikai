# The same exported unit name again, declared later in the stream:
# resolution must not favour whichever module was flattened first.
pub unit tick

pub fn imperial_mk() : Real<tick> = 5.0<tick>
pub fn imperial_ratio(x: Real<tick>) : Real = x / 1.0<tick>

# Two exported units of the same name are distinct atoms: a value of
# one module's `tick` must not feed a signature written against the
# other module's.
import metric
import imperial

fn main() : Unit / Stdout = {
  let a = metric.metric_mk()
  let b = imperial.imperial_ratio(a)
  Stdout.print("#{b}")
}

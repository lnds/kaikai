# m6.2 v2 regression: a `wrap` exported by box_b. Distinct C symbol
# (`kai_box_b__wrap`) from box_a's `kai_box_a__wrap`; the linker no
# longer rejects them as duplicates.

pub fn wrap(n: Int) : Int = n * 2

# A private protocol: only this module may dispatch through it.
protocol Stash {
  stash(x: Self) : Int
}

pub type Box = { v: Int }

impl Stash for Box {
  fn stash(x: Box) : Int = x.v
}

pub fn mk(n: Int) : Box = Box { v: n }

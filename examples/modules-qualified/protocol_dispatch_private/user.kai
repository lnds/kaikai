import vault

# This module's own `Stash` declares the op, so the bare call
# resolves; vault's private homonym has the only impl for `Box`,
# and it must NOT be a dispatch candidate here.
pub protocol Stash {
  stash(x: Self) : Int
}

pub fn peek() : Int = stash(vault.mk(9))

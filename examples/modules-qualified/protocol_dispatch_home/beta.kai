# The other half: identical protocol shape and type-head name.
# `go` must dispatch to this module's own impl, never alpha's.
pub protocol P {
  op(x: Self) : Int
}

pub type Vec = { b: Int }

impl P for Vec {
  fn op(x: Vec) : Int = x.b + 200
}

pub fn go() : Int = op(Vec { b: 2 })

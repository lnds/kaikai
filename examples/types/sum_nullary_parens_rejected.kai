# A nullary variant takes no parentheses. `X()` used to be the only way
# to spell a one-member sum; the leading bar replaced it, and the empty
# argument list is now rejected wherever it appears.

type Evidence = IdenticalBytecode()

fn main() : Int = 0

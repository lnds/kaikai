import loop

fn build_naive(n: Int) : String = {
  var acc = ""
  var i = 0
  while { @i < n } {
    acc := @acc ++ "frag"
    i := @i + 1
  }
  @acc
}

fn main() : Unit / Console = {
  let s = build_naive(50000)
  print("len=#{int_to_string(string.length(s))}")
}

# tier1-backend-parity skip list (issue #575).
#
# Format: <relative-path>:<marker>:<one-line-reason>
#   where <marker> is either an open tracking issue number (a real
#   parity bug — the skip is the bookmark, the issue is the work) or a
#   stable category tag for entries that are not bugs:
#     - exempt-nondet  — diverges by design (scheduler / RNG / clock);
#       will not converge, must NOT block Orongo.
#   The harness (tools/test-backend-parity.sh) keys only on the
#   `^<path>:` prefix, so the marker field is documentation, not parsed.
#
# This file is split into three labelled sections:
#   1. parity bugs (#622/#618)  — must empty before Orongo (1.0).
#   2. parity-exempt: nondet    — divergence is the fixture's contract.
#   3. harness categorization (#357) — library files, harness-mode and
#      package-mode fixtures the file-mode walk cannot run. NOT parity
#      bugs; a separate suite-categorization concern. (#626 — negative
#      tests in positive dirs — is CLOSED; see that section.)
# When a bug-issue closes, the closing lane removes its line and re-runs
# tools/test-backend-parity.sh to confirm parity is restored.
#
# For fixtures that are intentionally backend-specific by design, the
# inline `// skip-backend-parity: <reason>` annotation on the fixture's
# first line would also work — but `//` is not a valid kaikai comment
# (kaikai uses `#`), so for `.kai` fixtures the exemption lives here.

# ============================================================
# === parity bugs (must empty before Orongo) ===
# #622 — LLVM backend comprehensive parity divergence audit
# ============================================================
# Fixtures where C builds + runs cleanly but LLVM diverges by a real
# bug (segv, wrong exit code, wrong stdout). C is the maturity oracle;
# every line here is a site where the production LLVM backend is wrong
# and the C reference is right. The umbrella issue indexes these;
# sub-issues spawn per root-cause cluster. This section MUST be empty
# before the Orongo (1.0) edition ships.
#
# Resolved + removed by prior parity lanes (do NOT re-add):
#   - llvm-parity-A (#618): json_basic, json_surrogate_decode,
#     json_surrogate_encode — `\xNN` escape decoding in the emitter.
#   - llvm-parity-B (#558): issue_558, m7a_7_default_file,
#     m7a_7_default_stdin, m7a_7_default_env, os_env_mutate,
#     stdin_read_bytes — AST-driven default-handler install at main.
#   - llvm-parity-D / #706: reuse_record_basic, trace_prefix, the TCO
#     segvs. trace_basic + poker_dealer pass today and are unskipped
#     by the llvm-parity-cleanup lane (were stale, not bugs).

# m8_4_cancel_caught: RESOLVED (parity lane, refs #622). The LLVM
# `handle` lowering omitted the m7a #6e setjmp/longjmp landing pad, so
# a clause that discards `resume` (Cancel.raise() forces it) could not
# unwind out of the body — the tail ran to completion (prints "never
# reached", returns 99) instead of yielding the clause value 42. The
# fix emits the setjmp pad in `llvm_emit_handle`, installs via
# `kaix_evidence_push_with_jmp`, and longjmps from the op site on a
# discard (`llvm_emit_op_dispatch`), mirroring emit_c.kai. The discard
# + body-result slots are `volatile` (C99 §7.13.2.1 mirror). Both
# backends now print 42, no "never reached". Coverage: this fixture +
# cancel_discard_minimal (single-frame) + cancel_discard_nested
# (three-frame unwind, ASAN guard). Removed from the skip list.

# m8_fiber_stack_overflow: RESOLVED (#718). The LLVM backend now emits
# scalar (unboxed) function signatures for UFns — `define i64
# @kai_deep(i64 %kair_n)`, mirroring C's `int64_t kai_deep(int64_t)` —
# instead of boxed `%KaiValue* @kai_deep(%KaiValue*)` with a `kaix_int`
# heap box per op. The body lowers fully raw (the EIf/EBlock-tail raw
# arms in `llvm_emit_kind_raw` were the missing piece), so each frame
# is now as small as C's and 50k frames hold in the 64 KiB private
# fiber stack on BOTH backends. Sibling of #706 (LLVM-TCO): both were
# the LLVM emitter dropping a C-path codegen optimization. emit_llvm
# consumes `UFnSig` from fnreg (no analysis change); selfhost stays
# byte-identical (kaic2b.c == kaic2c.c) and selfhost-llvm deterministic
# (s1.ll == s2.ll). Coverage: this fixture (unskipped) +
# examples/perceus/scalar_fn_sig_deep_recursion.kai. This was the LAST
# line in the #622 parity-bugs section — it is now empty, so #622 can
# close alongside #718 for Orongo (1.0).

# ============================================================
# === parity-exempt: non-determinism (will not converge by design) ===
# ============================================================
# These diverge by DESIGN — a scheduler, RNG seed, or wall-clock
# property surfaces in stdout. Per Go/Erlang precedent, forcing
# byte-exact stdout across two backends here turns an implementation
# property into a spec one. They are NOT parity bugs and do NOT block
# Orongo. The inline `// skip-backend-parity` annotation is not used
# because `//` is not a valid kaikai comment (kaikai uses `#`) and the
# annotation must be on the fixture's first line, which would break the
# parse — so the exemption lives here instead.

# #706 closed the TCO segv (both backends run the 1M-insert loop to
# completion). The ONLY remaining divergence is the `elapsed: N.NNNs`
# wall-clock line — verified identical modulo that line by the
# llvm-parity-cleanup lane. Timestamp nondeterminism, exempt.
examples/perceus/rb_tree_bench.kai:exempt-nondet:`elapsed:` wall-clock timestamp differs; identical otherwise (verified)

# Both backends correctly trip the fiber guard page and exit 139 with the
# `kai: fiber stack overflow at <ptr>` diagnostic. The ONLY divergence is
# the `<ptr>` — a raw stack address printed by the SIGSEGV handler, which
# is a runtime memory address and cannot match byte-for-byte across two
# backends (or even two runs). Exit code and message text both match.
# (Surfaced when the fixture was hardened against the -O2 kaic2 flip so the
# recursion actually trips the guard page under optimisation.)
examples/effects/m8_fiber_stack_overflow.kai:exempt-nondet:`<ptr>` in the guard-page diagnostic is a raw stack address; exit 139 + message text match

# The default Random handler (runtime.h `_kai_pcg32_ensure_seeded`)
# seeds PCG32 from `time(NULL)` — one seed per wall-clock SECOND. The
# parity harness compiles + runs the C build, then the LLVM build, a few
# seconds apart, so the two draws use different seeds and the shuffled
# deck diverges. Verified time-seeded, not a backend bug: two runs of
# the SAME backend across a >1s gap also diverge, and within one second
# both backends agree. Both fixtures shuffle a deck via `Random`/shuffle
# and print the cards, so the divergence is the deck, not the codegen.
# (Will converge to a real parity check once SecureRandom-based seeding
# or a fixed test seed lands; until then the draw is the contract.)
demos/blackjack/main.kai:exempt-nondet:time-seeded Random shuffle; deck differs by seed-per-second, not by backend (verified)
demos/poker_dealer/main.kai:exempt-nondet:time-seeded Random shuffle; deck differs by seed-per-second, not by backend (verified)
demos/ping_pong/main.kai:exempt-nondet:three causally independent producers into one mailbox; arrival order is scheduler-dependent above one thread, same lines either way (verified)
examples/effects/issue_763_spawn_actor_policy_block_sender.kai:exempt-nondet:producer/consumer interleaving on a bounded mailbox; deterministic at one thread, 1/8 runs reorder at N=8 on both backends (verified)
examples/effects/issue_763_spawn_actor_policy_drop_oldest.kai:exempt-nondet:which message drop_oldest discards depends on producer/consumer timing; deterministic at one thread, 1-2/8 runs differ at N=8 on both backends (verified)
examples/effects/m8x_2_yield_interleave.kai:exempt-nondet:golden records the cooperative round-robin; independent yielding fibers interleave freely above one thread (verified)
examples/effects/m8x_9_nested_mailbox_under_trap_exit.kai:exempt-nondet:nested-mailbox trace order is scheduler-dependent above one thread (verified)
examples/effects/m8x_9_nursery_autojoin.kai:exempt-nondet:child completion order is scheduler-dependent above one thread (verified)
examples/llvm/cancel_raise_in_fiber_under_mailbox.kai:exempt-nondet:the child's raise print races main's park print; native@8 and c@8 produce the identical line multiset, differing only in that order (verified)
examples/effects/m8x_4_block_sender.kai:exempt-nondet:producer/consumer prints interleave above one thread; native@8 and c@8 share the line multiset, differing only in order (verified)
examples/quickstart/05_concurrent.kai:exempt-nondet:two producers print A/B concurrently; the interleave (and occasional torn line) is scheduler-dependent, multiset invariant (verified)
demos/stdin_concurrent/main.kai:exempt-nondet:reader-EOF line lands at a scheduler-dependent point relative to the compute fiber's prints above one thread (verified)
examples/effects/mn_print_line_atomic_hammer.kai:exempt-nondet:eight fibers race concurrent prints; cross-fiber order is scheduler-dependent by design — line atomicity is gated by the multiset checks in run-mn-determinism and run-mn-native-soundness (verified)

# The Log default handler stamps each line with the current wall-clock
# time (`[YYYY-MM-DDTHH:MM:SSZ]`). The C and LLVM builds run seconds
# apart, so the timestamp prefix differs; the message bodies are
# byte-identical (verified modulo the `T..Z` field). Clock nondet, exempt.
examples/effects/issue_141_log_default.kai:exempt-nondet:`[..Z]` timestamp prefix differs by run time; message bodies identical (verified)

# trmc_list_build_large: TRMC for the builtin cons (`[h, ...recur(t)]`)
# is lowered only by the C-text emitter today; on LLVM the cons-modulo-
# cons fn falls back to ordinary recursion, which overflows the stack on
# this 1M-deep build (C runs it in O(1) stack). The C and LLVM VALUES
# would match if both completed — this is a backend-coverage gap (LLVM
# cons-TRMC), not a value divergence. The small-list value-parity guard
# is examples/perceus/trmc_modcons_llvm.kai (runs + matches on both). A
# follow-up lane lowers the cons step on the LLVM/native backends and
# removes this line. Until then the large stress fixture is C-only,
# driven by stage2 `test-trmc-list-build`.
examples/perceus/trmc_list_build_large.kai:exempt-cons-trmc-llvm:LLVM lacks cons-TRMC lowering; falls back to ordinary recursion → overflows the 1M build (C O(1)). Values match; coverage gap, not divergence
examples/perceus/filter_heap_bounded_817.kai:exempt-native-filter-gap:#817 leak fixture is a C-backend KAI_TRACE_RC gate; the native backend has a pre-existing gap on the filter-over-heap shape (`kai: type mismatch in +`, same class as weather/main on main) — not a divergence from this fix. Parity converges when the native filter gap closes (separate burndown).

# ============================================================
# === harness categorization (#357) — not parity bugs ===
# #626 — CLOSED. Fixture-categorisation: negative tests in positive dirs
# ============================================================
# #626 emptied this section's negative-test backlog (PR closing #626):
#   - 16 compile-reject fixtures that lived in examples/effects/ (gated
#     by hand-rolled stage2/Makefile grep loops) MOVED to
#     examples/negative/{parser_syntax,effects_phase2,subsumption,
#     diagnostic_quality}/ with their .err.expected/.diag.expected
#     goldens. They now live where tools/test-negative.sh discovers them
#     and are excluded from this walk by directory (test-backend-parity
#     skips examples/negative wholesale). The Makefile loops are gone.
#   - ~20 fixtures the skip list mislabeled as "negative / aspirational /
#     rot" were verified to COMPILE + RUN + pass C↔LLVM parity today
#     (the rationale was stale): the ffi pair, issue_78, m8x_7, the four
#     perceus bug-shape repros (#82/#210/#297/#350 fixes landed), both
#     quickstart demos, the three stdlib "rot" cases (os_basic /
#     os_process_basic / time_clock_default — every "renamed/missing"
#     symbol exists), http_client_basic (in-process loopback), and every
#     demos/9d9l + demos/vs program. All were DE-LISTED; the parity
#     harness now runs them as ordinary positives and they pass.
# What remains below is NOT a #626 concern — it is the residual
# suite-membership category (#357): library files with no `main`,
# harness-mode fixtures (bench/check blocks), and package-mode fixtures
# the file-mode walk cannot resolve. These FAIL identically on BOTH
# backends (no `main` → link error, or manifest deps unresolved);
# nothing here is an LLVM↔C divergence.

# Package-mode fixtures: not buildable in this file-mode harness (which
# cannot resolve manifest deps). Their C<->LLVM parity IS verified —
# in package mode — by tools/test-packages.sh's `run_parity` pass. Not
# a coverage gap; a harness-axis split (file-mode here, package-mode
# there). See both scripts' headers (decided 2026-05-27).
examples/packages/simple_dep/main.kai:357:package-mode fixture; C<->LLVM parity covered by test-packages.sh run_parity
examples/packages/transitive/main.kai:357:package-mode fixture; C<->LLVM parity covered by test-packages.sh run_parity
examples/packages/auto_install/main.kai:357:package-mode fixture (git dep); C<->LLVM parity covered by test-packages.sh run_parity (parity-auto_install)

# Library files: no `main`, so a standalone file-mode build link-fails on
# BOTH backends (not a divergence). Imported by a sibling fixture the
# harness already runs.
examples/effects/foreign_prelude_lib.kai:357:library file with no main; imported by examples/effects/foreign_prelude.kai
examples/perceus/rb_tree.kai:357:library file with no main; only the rb_tree_bench wrapper has main
examples/perceus/modcall_step_1153.kai:357:library file with no main; imported by modcall_linear_1153.kai
examples/stdlib/qualified_call_in_prelude_lib.kai:357:library file with no main; imported by its sibling fixture
examples/stdlib/issue_219_typer_modcall_lib.kai:357:library file with no main; imported by its sibling fixture

# Harness-mode fixtures: only `bench { }` / `check { }` blocks, no `main`,
# so a file-mode build link-fails on BOTH backends. Run via `kai bench` /
# `kai check`, not as standalone entry points.
examples/stdlib/bench_basic.kai:357:bench-mode fixture; no main, run via `kai bench`
examples/stdlib/bench_median_mad.kai:357:bench-mode fixture; no main, run via `kai bench`
examples/stdlib/check_basic.kai:357:check-mode fixture; no main, run via `kai check`
examples/stdlib/check_shrinking.kai:357:check-mode fixture; no main, run via `kai check`

# ============================================================
# #618 — RESOLVED 2026-05-26 (llvm-parity-A)
# ============================================================
# The LLVM emitter dropped `\xNN` hex escapes in string literals:
# `json_byte_table()` in stdlib/encoding/json.kai is a `"\x01..\xff"`
# literal that `json_utf8_encode` slices into, so every JSON-decoded
# codepoint was corrupted under LLVM. Fixed in emit_llvm.kai
# `llvm_encode_body_loop` by decoding `\xNN` (advance cursor by 4).
# Removed: json_surrogate_decode, json_surrogate_encode (#618) and
# json_basic (#622) — all now byte-identical on C and LLVM.

# ============================================================
# #626 — Aspirational demos: DE-LISTED (PR closing #626)
# ============================================================
# The demos/9d9l/* and demos/vs/* programs were skip-listed as
# "aspirational; FAIL until <feature> lands". Verified 2026-06-11: every
# one BUILDS and passes C↔LLVM parity today (the features they waited on
# shipped). De-listed; the parity harness runs them as ordinary positives.
# (demos/9d9l/vectores panics `file.read: cannot open file` with no input
# file present — exit 1 identical on BOTH backends, so it still passes the
# parity check, which compares the two backends to each other.)

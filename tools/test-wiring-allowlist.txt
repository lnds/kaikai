# Allowlist for tools/audit-test-wiring.sh. One entry per line:
#
#   target:<make-target>:<reason>   — a test-* target that is deliberately
#                                     wired to no tier (dev convenience,
#                                     or CI reaches its script directly).
#   dir:<examples-subdir>:<reason>  — an examples/ level-1 directory with
#                                     .kai fixtures and no harness yet.
#
# A target entry that IS referenced by a tier fails the audit as
# stale, so target entries self-clean: wire the thing, drop the line.
# Dir entries do not expire (short basenames match incidental words).
target:test-fast:dev convenience aggregate, run by hand while iterating
target:test-namespace-collisions:collision corpus still has red-by-design dirs; run by hand until green, then wire into tier1
target:test-namespace-collisions-neg:collision corpus still has red-by-design dirs; run by hand until green, then wire into tier1
target:test-namespace-collisions-native:collision corpus still has red-by-design dirs; run by hand until green, then wire into tier1
target:test-namespace-collisions-asan:collision corpus still has red-by-design dirs; run by hand until green, then wire into tier1
target:test-namespace-collisions-testsym:collision corpus still has red-by-design dirs; run by hand until green, then wire into tier1
target:test-mn-corpus:local entry point; CI runs tools/run-mn-corpus-determinism.sh directly, sharded (tier1-mn-corpus.yml)
target:test-namespace-collisions-diag:collision corpus still has red-by-design dirs; run by hand until green, then wire into tier1
target:test-namespace-matrix:collision matrix is red by design until the resolver lands; run by hand
target:test-namespace-matrix-status:progress report over the collision matrix, not a gate; run by hand

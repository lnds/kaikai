# test-effects-goldens skip list.
#
# Format: <relative-path>:<marker>:<one-line-reason>
#   where <marker> is either an open tracking issue number (a real
#   golden divergence — the skip is the bookmark, the issue is the
#   work) or a stable category tag for entries that are not bugs:
#     - exempt-nonzero-exit — the fixture's contract is a deliberate
#       runtime abort (trap / fiber fault), so exit status is not 0;
#       its dedicated Makefile target gates stdout AND the expected
#       status together.
#     - exempt-nondet — output order is scheduler-dependent by design;
#       the M:N harnesses own its property.
#     - exempt-harness — needs an external driver (signals, pty); the
#       dedicated target supplies it.
# The harness (tools/test-effects-goldens.sh) keys only on the
# `^<path>:` prefix; the marker field is documentation, not parsed.
examples/effects/issue_1067_trap_top_level_exits.kai:exempt-nonzero-exit:trap exits 1 by contract (test-issue-1067-fiber-trap-isolation)
examples/effects/issue_1093_div_by_zero_traps.kai:exempt-nonzero-exit:trap exits 1 by contract (test-issue-1093-divzero-trap)
examples/effects/issue_1093_div_overflow_traps.kai:exempt-nonzero-exit:trap exits 1 by contract (test-issue-1093-divzero-trap)
examples/effects/issue_1093_mod_by_zero_traps.kai:exempt-nonzero-exit:trap exits 1 by contract (test-issue-1093-divzero-trap)
examples/effects/issue_978_spawn_parent_handled_effect.kai:exempt-nonzero-exit:fiber fault exits 1 by contract (test-issue-978-spawn-parent-handled-effect)
examples/effects/issue_997_spawn_cell_runtime_floor.kai:exempt-nonzero-exit:fiber fault exits 1 by contract (test-issue-997-spawn-cell-fiber-local)
examples/effects/mn_print_line_atomic_hammer.kai:exempt-nondet:fiber interleaving order is scheduler-dependent; M:N harnesses own the atomicity property
examples/effects/issue_107_signal_trap.kai:exempt-harness:waits for an externally delivered signal; its dedicated target drives it
examples/effects/m8x_signal_await_parks.kai:exempt-harness:waits for an externally delivered signal; its dedicated target drives it

# RC self-host detector corpus — fixtures in examples/perceus/, one per line.
#
# Two shapes drove the native self-host RC bug chain; both are covered so a
# regression in either lowering path is caught the day it lands:
#
#   Family A — alias binders nested in a match arm (variant/record/list
#   heads). The native lowering's ad-hoc binder collectors missed a
#   pointer-kind sub-binder, so a consuming callee or a self-tail scrutinee
#   drop freed a borrowed slot the arm still read.
#
#   Family B — TRMC / reuse-cons over a shared pass-through arg
#   (`[h, ...f(rest, shared)]`). A perceus/emit pair disagreed on whether
#   the shared arg was transferred raw, planting a drop that over-decref'd
#   it once per iteration.

# Family A — nested alias binders
selftail_nested_variant_binder_1025
variant_nested_discriminant_dup_1025
native_variant_reuse_double_use_872
match_shared_tag_subdiscrimination
gap3_tail_rebuild_variant
gap3_tail_rebuild_record
native_variant_real_slot_uaf_747

# Family B — TRMC / reuse-cons with a shared arg
trmc_spread_shared_arg
cons_reuse_shared_donor_1025
cons_reuse_embed_unique_1025
native_cons_selftail_leak_860
native_shared_reuse_corruption_995
reuse_projection_scrutinee_1069

# C-emit hoisted sub-pattern load — a smaller sibling ctor through a
# catch-all arm must not be read at a hoisted slot past its block
match_hoist_smaller_ctor_1073

# Nested rotation reuse — the dual-branch fork (outer overwrite + inner
# token donation) on the balance rebuild shapes
nested_rotation_reuse_1053

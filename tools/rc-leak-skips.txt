# rc-leak-gate skip list.
#
# Format: <name>:<marker>:<one-line-reason> — <marker> is either an open
# issue number (a real finding parked while its issue is worked) or a
# stable category tag for fixtures the gate cannot execute standalone:
#   - negative     — the fixture is a compile-error golden (.err.expected);
#     no binary exists to run.
#   - library      — a module with no `main`, exercised through an importing
#     fixture that the gate does measure.

borrow_caret_expr_neg_1127:negative:compile-error golden, no binary
borrow_caret_lambda_neg_1127:negative:compile-error golden, no binary
fixed_vec_collect_unprovable_1480:negative:compile-error golden, no binary
modcall_step_1153:library:no main; driven by modcall_linear_1153
rb_tree:library:no main; the red-black tree driven by rb_tree_bench

# Per-backend skips: `-` in that column of the baseline. Format
# <name>:<backend>:<marker>:<reason>.
rboxed_prim_shared_let_1648:native:link:kaix_core_llvm_backend_tag is unresolved in the native link
rb_tree_bench:native:nondet:the elapsed-time string's length feeds the count; native keeps those allocs, C frees them

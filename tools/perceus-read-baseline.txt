# Callers of `pcs_read_table`, one `<file> <fn>` per line (tools/perceus-read-audit.sh).
# perceus_decl_b builds the table once per declaration; the tcrec goto ledger
# and the KIR reuse lowering still re-collect over the rewritten body.
emit_c.kai tcrec_rewrite_decl
emit_c.kai tcrec_rewrite_pcs_ret_wrap
kir_lower_walk.kai reuse_gc_uses_exprs
perceus.kai perceus_decl_b

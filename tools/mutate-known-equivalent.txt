# Known-equivalent mutants — mutations that mean exactly what the
# original meant and so can never be killed.
#
# A suppression here removes a mutant from the run. That is only honest
# when the mutant is genuinely equivalent, so every entry states WHY in
# its own words. An entry without a reason is a hard error, not a
# suppression: one nobody justified is indistinguishable from a hole
# somebody hid, which is the same discipline the coverage matrix applies
# to its `N/A:` rows.
#
# Format, one per line:
#
#   <file>:<line>:<operator>   # why this mutant cannot be killed
#
# `file` is written exactly as `kai mutate` reports it. `operator` is one
# of: arm, compare, connect, negate, literal, call.
#
# This file suppresses; it does not excuse. A mutant that survives
# because the suite has a hole belongs in a fixture, not here.

stage2/compiler/type_scope.kai:67:arm   # the compiler declares no test blocks, so no oracle over its fixture corpus can reach a DTest arm; this is unkillable by construction, not an untested case

# core.string: boundary identities exercised by string_boundaries_test.kai.
# Keep line numbers aligned with the native mutation report; do not suppress
# line 329, whose two comparisons are both checked by the inline advance test.
stdlib/core/string.kai:67:compare   # at i == n, char_at(s, n) is None and the match returns the same n
stdlib/core/string.kai:79:compare   # at i == 0, char_at(s, -1) is None and the match returns the same 0
stdlib/core/string.kai:95:compare   # at hi == lo, string_slice(s, lo, 0) equals the empty-string branch
stdlib/core/string.kai:157:compare   # at n == width, repeat(fill, 0) is empty and concatenation preserves s
stdlib/core/string.kai:164:compare   # at n == width, repeat(fill, 0) is empty and concatenation preserves s
stdlib/core/string.kai:172:arm   # after the preceding empty-string arm, [x] also matches [x, ...rest] with rest == []; recursing on [] returns [], hence the same [x]
stdlib/core/string.kai:200:compare   # off is in range, so string_cp_len is 1..4 even on malformed bytes; w == 0 is unreachable
stdlib/core/string.kai:200:literal   # changing threshold 0 to 1 selects the same step 1 when w == 1; changing fallback 1 to 2 is unreachable because in-range w is positive
stdlib/core/string.kai:251:compare   # off is in range, so string_cp_len is 1..4 even on malformed bytes; w == 0 is unreachable
stdlib/core/string.kai:251:literal   # changing threshold 0 to 1 selects the same step 1 when w == 1; changing fallback 1 to 2 is unreachable because in-range w is positive
stdlib/core/string.kai:282:compare   # at off == 0, a non-continuation returns 0; a continuation recurses to -1 and clamps to 0
stdlib/core/string.kai:287:compare   # at off == n, the out-of-range byte sentinel -1 is not a continuation and returns n
stdlib/core/string.kai:298:compare   # at byte_off == 0, the empty input returns n == 0 and other inputs call the helper which clamps to 0
stdlib/core/string.kai:299:compare   # at byte_off == n, the helper reads sentinel -1, not a continuation, and returns n
stdlib/core/string.kai:309:compare   # at byte_off == n, string_ceil_loop repeats the same clamp and returns n
stdlib/core/string.kai:332:compare   # off is in range, so string_cp_len is 1..4 even on malformed bytes; w == 0 is unreachable
stdlib/core/string.kai:332:literal   # changing threshold 0 to 1 selects the same step 1 when w == 1; changing fallback 1 to 2 is unreachable because in-range w is positive
stdlib/core/string.kai:344:compare   # at len == 0, advance returns lo and the resulting zero-byte slice is empty
stdlib/core/string.kai:424:compare   # off is in range, so string_cp_len is 1..4 even on malformed bytes; w == 0 is unreachable
stdlib/core/string.kai:424:literal   # changing threshold 0 to 1 selects the same step 1 when w == 1; changing fallback 1 to 2 is unreachable because in-range w is positive
stdlib/core/string.kai:433:compare   # at i == n, char_at(s, n) is None and that arm returns the same true
stdlib/core/string.kai:439:literal   # the preceding i >= n guard and initial i == 0 guarantee an in-range byte offset; char_at is Some even for malformed bytes, so the None result is unreachable

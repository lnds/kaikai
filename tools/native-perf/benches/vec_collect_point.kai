type Point = { x: Int, y: Int }

fn sum_x(v: Vec[Point], i: Int, n: Int, acc: Int) : Int =
  if i >= n { acc } else { sum_x(v, i + 1, n, acc + vec_get(v, i).x) }

fn main() : Int / Stdout = {
  let n = 10000000
  let v: Vec[Point] = [1..n] | (k => Point { x: k, y: k * 2 })
  Stdout.print(int_to_string(sum_x(v, 0, n, 0)))
  0
}

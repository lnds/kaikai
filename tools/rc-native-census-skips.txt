# rc-native-census skip list.
#
# Format: <dir>/<name>:<marker>:<one-line-reason> — <marker> is either an
# open issue number (a real finding parked while its issue is worked; the
# skip is the bookmark, the issue is the work) or a stable category tag
# for fixtures the census cannot execute standalone:
#   - dump-golden     — .out.expected is a compiler-analysis dump, not
#     program stdout; running the binary can never match it.
#   - signal-harness  — the program parks waiting for an EXTERNAL signal
#     that a Makefile harness sends; standalone execution times out.
# When an issue-marked line's issue closes, the closing lane removes the
# line and re-runs tools/rc-native-census.sh to confirm the fixture is
# clean.

perceus/borrowsafe_classify:dump-golden:golden is --dump-borrowsafe analysis output
perceus/purity_scalar:dump-golden:golden is purity-classifier analysis output
perceus/rawsafe_boxed:dump-golden:golden is --dump-rawsafe analysis output
perceus/rawsafe_bump:dump-golden:golden is --dump-rawsafe analysis output
effects/issue_107_signal_trap:signal-harness:waits for SIGTERM from the sigharness runner
effects/m8x_signal_await_parks:signal-harness:Signal.await parks until an external signal arrives
perceus/let_bound_big_int_multiuse:1639:UBSan signed-shift in kaix_core_bit_shl, native mirror of 1637

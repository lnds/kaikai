type T = N(Int) | C(T, T)
fn build(d: Int) : T = if d <= 0 { N(d) } else { C(build(d - 1), build(d - 1)) }
fn sum(t: T) : Int = match t { N(v) -> v  C(a, b) -> sum(a) + sum(b) }
fn main() : Int { println("#{sum(build(6))}") 0 }

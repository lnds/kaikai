# Project Euler #3: largest prime factor of 600851475143.

fn largest_pf(n: Int, f: Int) : Int = {
  if f * f > n { n }
  else if n % f == 0 { largest_pf(n / f, f) }
  else { largest_pf(n, f + 1) }
}

fn main() = println("Euler 3: #{int_to_string(largest_pf(600851475143, 2))}")

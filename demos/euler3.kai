module Euler3

fn factor_of(f, n: Num) := (n % f) == 0

fn prime_factor(f, n: Num): [Num] {
    switch {
        case n == 1: []
        case factor_of(f, n): [f, prime_factor(f, n//f)...]
        default: prime_factor(f+1, n)
    }
}
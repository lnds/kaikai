module FizzBuzz

fn fizzbuzz(i: Int) := switch {
    case i % 3 == 0: IO.println("#{i} FIZZ")
    case i % 5 == 0: IO.println("#{i} BUZZ")
    default: IO.println(i)
}

fn main() {
    0..1000 | fizzbuzz
}

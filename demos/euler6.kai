module Euler6

fn sum_n(n: Num) :=  ((n+1) * n)/2

fn square_sum(n: Num) = {
    let s := sum_n(n)
    s*s
}

fn sum_n_square(n: Num) := 1..n | \{x -> x * x}  | sum

fn diff_squares(n: Num) := square_sum(n) - sum_n_square(n)

fn main() {
    diff_squares(100) | IO.println()
}
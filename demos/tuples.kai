module Tuples

test "tuples" {

    tuple1 := {:a, :b}
    tuple2 := {:a,} # without the , is not a tuple

    va := :a

    ca := {:a}

    assert va == ca

    val := :a

    ta := {:a,}

    assert va != ta

 }

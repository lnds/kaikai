module Euler2

let fib_seq := fib (0, 1)

fn fib (a, b: Int) := a, fib(b, a+b)...

let even := \{x -> x % 2 == 0}

# IO defines println() as perform println!(arg)

fn main() {
    fib_seq
        | takeWhile(\{x -> x < 4_000_000})
        | filter(even)
        | List.sum
        | IO.println
}
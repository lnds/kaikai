fn fizzbuzz(i: Int) : String = match i {
  n if n % 15 == 0 -> "FizzBuzz"
  n if n % 3 == 0  -> "Fizz"
  n if n % 5 == 0  -> "Buzz"
  n                -> int_to_string(n)
}

fn main() = [1..15] | fizzbuzz |> each(println)

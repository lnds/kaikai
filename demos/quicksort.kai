module QuickSort

fn sort(list) = {
    [head, tail...] := list
    lesser := tail |> List.filter(\x -> x < head) |> sort
    greater := tail |> List.filter(\x -> x > head) |> sort
    lesser ++ head ++ greater
}
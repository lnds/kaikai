fn str_imc(peso: Real, altura: Real) : String = {
  let imc = peso / (altura * altura)
  match imc {
    n if n <= 18.5 -> "bajo peso"
    n if n <= 25.0 -> "normal"
    n if n <= 30.0 -> "sobrepeso"
    _              -> "obeso"
  }
}

fn main() = println(str_imc(70.0, 1.75))
